# S9 · 性能三基准点移植与透明对比 · 详细实现方案

> 承接 [moonbit-重写-roadmap-详细分析](./moonbit-重写-roadmap-详细分析.md) §4.2 的 **S9**
> （性能：移植三基准点 V03H/V10H/V40H，输入 `https://example.com/`，透明对比、无硬门槛），
> 收敛 roadmap §4.4 里程碑 **M3（性能与分发基线）**。本方案在 S8 已合入（公共层文件归位 +
> 入口刷新，测试 109，双后端全绿）的现状下，**动手前先读代码与文档、盘点性能前置事实、核对
> 参考基准语义、评估 MoonBit 侧可行载体与对比口径**，给出 S9 的详细实现方案与验收
> （不写实现代码，供 S9 实现阶段直接执行）。
>
> 日期：2026-09-06　｜　前置：S8 已合入 main（测试 109）；本文为**方案评估文档**，纯文档 + 目录/索引同步，
> 不做逻辑代码改动（S9 代码/基准落库按本文清单另行实现提交）。

---

## 0. 一句话结论

S9 = 把参考库 fast_qr v0.14.0 的 **criterion 三基准点 V03H/V10H/V40H**（输入固定 `https://example.com/`，
criterion 200 样本 × 10s）在本仓库移植出可复跑的**基准**并产出**透明对比数字**，收敛 M3「三基准点可跑并给出
对比数字；后端/产物分发结论记录在案」。关键评估结论：

1. **参考数字是 Rust `native` 的 82.2/269.3/2436.2 us**；而本环境 **`native` 后端不可用**（无 C 编译器，
   AGENTS §二.3 明令不把 native 写进 CI 必选）。因此 S9 的「对比」分两层：
   - **跨后端（可落地）**：wasm-gc / wasm 双后端各自跑三基准点，横向比体积/耗时（同源码、语义互证），
     复刻并升级 docs/wasm §5.2 的朴素微基准口径到「真实 QR 生成路径」；
   - **跨语言（需 Rust/native 环境）**：与参考 82/269/2436 us 的「透明对比」必须在一个**具备 native 的
     环境**（如装了 C 工具链 + fast_qr 的机器/CI）执行才有意义。roadmap §4.2 明言「透明对比（无硬门槛）」，
     S9 不做「追上 82us」的硬性门槛，只做数字透明 + 口径说清。
2. **载体**：MoonBit 无内置 criterion；本仓库现状无任何 bench 机制（`_bench_test.mbt` 仅 core 参考文档
   提及、顶层 `bench/` 基座为 P2 待办）。S9 最小可行 = 一个**可复跑的独立基准包/命令**（如 `cmd/bench`
   或 lib 内 `*_bench_test`），宿主侧计时；不空建顶层 `bench/`（沿用 roadmap「出现信号再拆」）。
3. **优化做不做**：S9 定位是「测出当前数字 + 透明对比」，**不是**大改性能。S5/S6 已列多条非阻断优化
   （§2.2）留 S9——S9 只承接其中**零风险/独立**的微优化并在基准前后各测一次做佐证；涉及正确性敏感的主
   循环重构（如 8 轮 clone→就地翻转）**不并入 S9**，单列 P2 待后续专项，避免破坏快照对齐。
4. **版本语义待实现时核对**：三基准点同输入、只差版本，需强制 `.version(V03/V10/V40)` × `.ecl(H)`；
   V03H 恰为 19 字节 Byte-H 的最小适配版本（V03 Byte-H 容量 24 ≥ 19），V10/V40 为强制升版（§3.1 论证）。

---

## 1. 范围界定（对照 roadmap §4.2 / M3 / 参考基准）

roadmap §4.2 的 S9 行：

> | S9 | 性能：移植三基准点 V03H/V10H/V40H（输入 `https://example.com/`） | `benches/qr.rs` | 透明对比（无硬门槛） |

roadmap §4.4 里程碑 **M3**：

> | M3 | 性能与分发基线 | 三基准点可跑并给出对比数字；后端/产物分发结论记录在案 |

参考 fast_qr 官方性能契约（docs/移植参考/fast-qr-接口.md §4，criterion 200 样本 × 10s）：

| 基准点 | fast_qr(native) | qrcode crate | 倍率 |
|------|------|------|------|
| V03H | 82.2us | 535.0us | 6.51x |
| V10H | 269.3us | 2114.5us | 7.85x |
| V40H | 2436.2us | 18037us | 7.40x |

**S9 范围锁定**：

### 1.1 S9 做（本方案落地清单）
- **S9-1 基准载体落地**：新增一个可复跑的基准入口（推荐 `cmd/bench`，§4.1 决策），对
  `QRBuilder::from_string("https://example.com/").ecl(H).version(V03/V10/V40).build()` 做重复迭代，
  宿主侧计时输出三点耗时/吞吐。
- **S9-2 跨后端数字**：在 wasm-gc / wasm 双后端各自跑通三基准点，记录耗时与产物体积，横向对比
  （同源码语义互证，复刻 wasm §5.2 升级到真实 QR 路径）。
- **S9-3 透明对比 + 口径**：在**具备 native + fast_qr 的环境**补一次 native MoonBit 对 fast_qr native 的
  三基准点对比，给出数字与倍率；数字透明、无硬门槛。当前环境（无 native）则先记录 wasm 数字 + 明确
  「native 对比待具备工具链环境执行」的方法学与跑法（§4.3）。
- **S9-4 承接零风险微优化**（§2.2 中无正确性风险的项）并在基准前后各测一次佐证。
- **S9-5 文档/索引**：README「文档」表新增本方案；roadmap §4.2/§4.4 记 S9 状态；M3 达成记录在案。

### 1.2 S9 不做（明确排除）
- **不做 8 轮 clone→就地翻转等主循环重构**（S5 O1）：正确性敏感、易破坏快照，单列 P2 专项；
  S9 只测基线，不为「追数字」引入风险。
- **不新增顶层 `bench/` 基座**：沿用「出现信号再拆」（现仅需 1 个入口 + 宿主计时脚本，不构成多包基座）。
- **不做 native 阶段进 CI 必选**（AGENTS §二.3：native 需 C 工具链，本镜像无）。
- **不做 wasm 体积/性能专项优化**（roadmap P2 优化链）；S9 只记录现状、不调 profile。
- **不改任何公共 API / 快照对齐**（S9 是性能测量，非功能改动；回归须维持 109 全绿 + 快照零差异）。

---

## 2. 现状盘点（动手前已读代码 + 文档）

### 2.1 可用产物与后端事实（实测/文档）
- 模块 `supported_targets = "+wasm+wasm-gc"`，`preferred_target = "wasm-gc"`；**无 native**（需 C 编译器，
  当前镜像未装，AGENTS §二.3）。兼容产物 `--target wasm`（WASI preview1）。
- 公共入口就绪：`QRCode::build`（自动/固定 mask）、`QRCode::build_fixed`、`QRBuilder`（new/from_string +
  mode/ecl/version/mask 链式 + build），可强制 ecl/version/mask——**S9 三基准点可直接用 QRBuilder 强制构造**
  （参考 benches/qr.rs 用 `QRBuilder::new(input).ecl/mode/version().build()`）。
- `cmd/main` 已有 demo 真码（`from_string("https://example.com/").build()`），可作基准迭代的原型参考，
  但只 build 一次、不循环计时。
- 早期 wasm 微基准（docs/wasm §5.2）用的是**独立 `/tmp/bench` 模块（不入库）+ 宿主侧 `time` 多次取最小值**，
  非严格 bench，仅供选型。S9 把该朴素口径升级为**入库存量、可复跑**。

### 2.2 已列的非阻断性能建议（S5/S6 评审遗留，S9 承接盘点）
| 来源 | 建议 | 正确性风险 | S9 承接 |
|------|------|-----------|---------|
| S5 O1 | `create_auto_qr` 每掩码 `apply_mask` 全量 copy + `score` 多趟扫描，V40 下 8 轮成本可观 | **高**（主循环重构） | 不并入，单列 P2 |
| S6 评审 §3 | V40 单行 hex 分块/压缩（快照测试文件） | 低（仅测试文件） | 不属性能热点，跳过 |
| S7 评审 §4-4 | `SvgBuilder::to_str` 循环里反复读 `qr.size`，可提局部变量 | 无 | 输出层非本 S9 三基准点路径，跳过 |
| S9 实测后 | 待 S9 基准跑出分布后再定 | — | S9 只测基线 + 记录 |

> 结论：S9 承接的微优化**极少**（三基准点核心路径是 encode→structure→placement→score→build，
> 无低风险可安全优化项）；S9 主交付 = **测准基线 + 数字透明**，不做性能改写。若基准暴露某热点有
> 零风险等价改写（如把反复调用的 `size` 提到局部变量），可顺手做并在基准前后各测一次佐证。

### 2.3 三基准点路径（热点对应参考性能决策）
三基准点主路径 = `QRCode::build` 编排：`select_capacity`(强制版本时直通) → `encode`(data_encoding) →
`structure`(reedsolomon 交织) → `create_fixed_qr`/`create_auto_qr`(matrix 放置 + 掩码 + score 择优)。
- **V03H/V10H/V40H 是否走自动择优**：参考 benches/qr.rs 通常**不指定 mask** → 走自动择优（8 轮 clone+score），
  这是 V40H 2.4ms 的主要成本。实现时核对参考是否固定 mask；默认按**自动择优**（不固定 mask）移植，
  与参考默认一致。
- 版本越大，放置区越大、8 轮 score 扫描越大 → V03 < V10 << V40 的耗时单调上升符合直觉。

---

## 3. 参考基准语义核对与关键决策

### 3.1 三基准点怎么构造（版本语义）

官方三基准点**共用同一个输入** `https://example.com/`（长度 19，全小写字母 → Byte 模式），只差版本号。
byte-H 容量表（internal constants，版本序号 = 版本-1）：V01=7 / V02=14 / **V03=24** ≥ 19 / …
故：
- **V03H**：19 字节 Byte-H 的**最小适配版本即 V03**——强制 `.version(V03)` 或自然（V03）等价；
- **V10H / V40H**：19 字节远小于这两版 Byte-H 容量（V10-H=119、V40-H=1273），**必须强制
  `.version(V10/V40)`** 才能体现大矩阵放置 + 8 轮 score 择优成本。

> **实现时核对点**：参考 `benches/qr.rs` 是否对三点都显式 `.version(Vxx).ecl(H)`、是否设 mask。
> 本方案推荐语义：`QRBuilder::from_string(input).ecl(ECL::H).version(Vxx).build()`（mask 留 None 走自动择优，
> 与参考默认一致）；V03H 的 `.version(V03)` 显式写，保证与参考「指定版本」路径同构、可复现。

### 3.2 对比口径评估（native 不可用 → 透明对比的分层）

**事实**：参考 82.2/269.3/2436.2 us 是 **Rust native + criterion** 数字；MoonBit 的 `native` 后端需 C 编译器，
当前环境不可用。直接把 wasm-gc 的 us 数与 Rust native us 数画等号是不科学的（解释器/GC 语义差异、宿主启动
开销）。因此「透明对比」必须**说清口径**，roadmap 亦无硬门槛：

| 对比层 | 可比性 | S9 落地 |
|--------|--------|---------|
| MoonBit wasm-gc vs wasm（同源码） | **高**（同源码语义互证，host 计时同口径） | S9-2 落地 |
| MoonBit native vs fast_qr native（需工具链） | **中**（都 native，criterion 口径需对齐样本/迭代） | S9-3 落地（方法学记录） |
| MoonBit wasm vs fast_qr native | **低**（语义层不同，仅量级参考） | 只在文档明示量级、不混为同口径 |

> **结论**：S9 核心承诺 = 「三基准点在可跑后端给出**可复现数字** + 对比口径透明」；
> 「与 Rust native 同口径对比」作为**方法学与跑法**记录，供具备工具链的环境执行（S9-3）。

### 3.3 决策 D14：基准载体 = `cmd/bench`（不入库或按需入库，见 §4.1）

- MoonBit 无 criterion；最贴近参考「重复迭代 + 取中/最小」的 = 一个**可执行命令**循环 build N 次 +
  宿主侧计时。放 `cmd/bench/`（`pkgtype(kind:"executable")`）与 `cmd/main` 并列，语义清晰、可
  `moon run cmd/bench --target <后端>`。
- 不空建顶层 `bench/`（roadmap「出现信号再拆」，现仅 1 入口 + 计时脚本，不构成多包基座）。

### 3.4 决策 D15：计时口径（宿主侧，对齐 wasm §5.2）

基准命令内部不内置纳秒钟（跨后端 host 计时更一致），由**宿主**（`time` / 计时脚本）对
`moon run cmd/bench --target <后端>` 整程计时；命令内用 `@time`（若 MoonBit 提供）仅作粗略节拍，主口径靠
宿主重复 run 取多次最小值（剔除冷启动，wasm §5.2 已用此法）。迭代数 N 由命令行参数传入（V40 大、需少
迭代；V03 小、需多迭代，避免单次 <1ms 计时噪声）。

### 3.5 优化承接策略（D16）：S9 只测基线，不做主循环改写

S5 O1（8 轮 clone→就地翻转）等正确性敏感重构**单列 P2 专项**，不进 S9。S9 只承接「把 `size`/版本等
循环外提」这类零风险等价改写（若基准暴露），并在改写前后各测一次做佐证，回归 109 全绿 + 快照零差异。

---

## 4. 逐文件落地清单

| # | 文件/动作 | 内容 | 验收要点 |
|---|-----------|------|---------|
| 1 | `cmd/bench/moon.pkg` + `main.mbt`（新） | 基准可执行包：循环 N 次 `QRBuilder::from_string(input).ecl(H).version(Vxx).build()`（V03/V10/V40 三点，N 可参数化），只计时不计输出 | 三点均可 `moon run` 跑通；强制版本语义正确；输出耗时可复现 |
| 2 | `scripts/bench.sh`（新） | 宿主侧计时脚本：对 wasm-gc/wasm 各后端、各基准点 run 多次取最小值，输出 markdown 表 | 复刻 wasm §5.2 的「多次取最小」口径；跑一次即产一张结果表 |
| 3 | S9 基准跑测 | 在可用后端（wasm-gc/wasm）跑出当前基线数字 | 记录 us 级耗时 + 产物体积；与快照零差异（正确性未破） |
| 4 | （可选）native 对比记录 | 在具 C 工具链 + fast_qr 环境按 §3.2 跑 native MoonBit vs fast_qr | 数字记录在案；不设门槛 |
| 5 | `docs/S9-性能基准-实现记录.md`（实现阶段生成） | 记录基准结果、口径、是否做微优化 | 数字透明、口径说清、可复现 |
| 6 | `README.md` | 「文档」表新增本方案；可选「性能」小节补 S9 结论摘要 | 无死链；树形图/索引与实码一致 |
| 7 | `docs/moonbit-重写-roadmap-详细分析.md` | §4.2 S9 行旁注「方案见本文」；§4.4 M3 状态补 S9 | roadmap 可回溯到本方案 |

> **实现顺序**：① 基准包 + 计时脚本（1/2，可独立跑）→ ② 跑测出数字（3/4）→ ③ 记录（5）→ ④ 文档
> 治理（6/7）→ ⑤ 收尾回归（AGENTS §二.4；native 不进 CI）。全程不改公共 API、不加 internal、不做主循环
> 重构，故基准本身是测量而非行为改动，回归 109 全绿 + 快照零差异即安全。

### 4.1 cmd/bench 是否入库？
- **建议入库**（与 `cmd/main` 并列），作为「可复跑基准」常驻，供后续优化反复对比——这与 S9「M3 性能与
  分发基线」吻合。若团队倾向「基准不入 main 产物面」，可放 lib 下 `*_bench_test.mbt`（core 参考机制），
  但该机制属 P2 待办、现无基座，故**首选 cmd/bench** 最小可行。
- 回滚：`git revert` 即复原；基准包不影响公共 `.mbti`（非 lib 公共符号）。

### 4.2 风险与回滚
- **风险低**：S9 = 新增独立基准命令 + 宿主计时脚本 + 文档，不触碰任何公共 API / 内部管线 / 快照对齐。
- 需盯的 2 点：① 基准强制版本语义必须与参考 benches/qr.rs 一致（§3.1 实现时核对）；② 计时口径跨后端
  要统一（多次取最小、剔冷启动），否则数字不可比。
- **回滚**：任一文件 `git revert`；基准包不入 `.mbti`，不影响对外接口。

---

## 5. 验收策略

| 层级 | 验收项 | 通过标准 |
|------|--------|---------|
| 结构 | 基准入口 + 计时脚本 | `cmd/bench` 三基准点可 `moon run`；`scripts/bench.sh` 产 markdown 表 |
| 语义 | 三基准点强制版本 | V03H 自然=V03、V10/V40 强制升版且 Byte-H 容量均富余（19<<24/119/1273）；与参考 benches/qr.rs 语义核对一致 |
| 接口护栏 | 公共 `.mbti` | 新增 cmd/bench 不影响 lib `.mbti`；公共 API 零漂移 |
| 回归 | AGENTS §二.4 | `moon fmt/info/check --deny-warn/test` 全绿；wasm-gc/wasm release 构建 + test 通过；109 全绿、快照零差异 |
| 透明对比 | 数字 | 三基准点 wasm-gc/wasm 各产出耗时 + 体积；native 对比方法学记录在案（具工具链环境执行） |
| 文档 | README/roadmap | 索引新增本方案、无死链；roadmap §4.2/§4.4 M3 记「基准已落/数字在记录」 |

> 说明：本容器无 `moon`（无 cargo/rustc），基准跑测需在具备 MoonBit 工具链（+ 可选 C 编译器做 native）
> 的环境执行；本文先固化方案、载体与验收语义，实现时按 AGENTS §二.4 + 本清单跑通再合入。

---

## 6. 汇总

1. **读了代码与文档**：盘点 S8 落地后 lib/internal/cmd/main 全貌、公共入口（QRBuilder 可强制版本）、
   可用后端（wasm-gc/wasm、无 native）、已列性能建议（S5 O1 等）。
2. **读了 roadmap 并评估 S9**：S9 = 移植三基准点 V03H/V10H/V40H（输入固定、强制版本 + ECL H）、
   透明对比（无硬门槛）、收敛 M3。核心障碍 = **native 不可用**，故对比分层（wasm 内可落地 + native
   对比留具工具链环境），口径透明即可。
3. **S9 定方案**：落地 `cmd/bench` 基准命令 + 宿主计时脚本（D14/D15），wasm-gc/wasm 双后端跑出
   基线数字；只测基线、不并入主循环重构（D16，S5 O1 单列 P2）；记录 + README/roadmap 治理。回归
   109 全绿 + 快照零差异即安全。

**S9 后续（实现阶段首个动作）**：在具备 MoonBit 工具链环境建 `cmd/bench` + `scripts/bench.sh`，
按参考 benches/qr.rs 核对三基准点强制版本语义后跑 wasm-gc/wasm 基线，记录数字；可选在装 C 工具链 +
fast_qr 的环境补 native 对比。S9 达成即 roadmap M3（性能与分发基线）收口。

---

## 7. 参考
- [moonbit-重写-roadmap-详细分析.md](./moonbit-重写-roadmap-详细分析.md) §4.2/§4.4（S9 行、M3、S5 O1）
- 参考 fast_qr v0.14.0（`53e8c99`）：`benches/qr.rs`、`src/qr.rs`（QRBuilder/QRCode）、`Cargo.toml` release
- [fast-qr-接口.md](./移植参考/fast-qr-接口.md) §4（性能契约：82.2/269.3/2436.2 us + 倍率）
- [跨语言重写评估.md](./移植参考/专有概念/跨语言重写评估.md) §5（三基准点移植 + 三处性能陷阱）
- [wasm-编译与运行-结果分析.md](./wasm-编译与运行-结果分析.md) §5.2/§6.1（宿主计时口径、native 不可用）
- 本仓库实码 `lib/qr_builder.mbt`/`lib/qr_build.mbt`/`cmd/main/main.mbt`、`lib/internal/*`
- S5/S6/S7 方案与评审记录（性能建议、口径勘误）
