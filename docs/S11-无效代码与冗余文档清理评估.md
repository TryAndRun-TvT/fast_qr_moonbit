# S11 — 无效代码与冗余文档清理评估（盘点 → 分类 → 处置）

> 本文回应 Issue #63：「阅读代码 && 文档；评估仓库中的大量无效代码 && 文档；给出详细分析与清理方案」。
>
> 定位：**评估报告 + 处置队列**，不是一次性大扫除的执行单。
> 所有结论都附**可复跑的判定命令**，可独立复核；任何删除动作都需按 §7 的顺序单独提 PR。
>
> 日期：2026-09-14（**v3 落地**）　｜　基线：`main@533e4cb` 起，v3 落地后 `moon test` **146 全绿**（default + `wasm-gc` 双跑）
> 参考检出：fast_qr v0.14.0 `53e8c99`（钉版）
>
> **v3 落地摘要（2026-09-14，Issue #63 第三轮：实做 P0/P1）**：§10.6 队列的 **P0-1/P0-2/P0-3/P1-1/P1-2/P1-3
> 已全部落地**（净效果见 **§11**）：删 8 个冗余符号、消 **2 处**双份实现
> （`build`↔`build_fixed`、`line_score`↔`line_scan_buf`）、补 `QRCode::empty` 出口、
> 补 6 类负向/回环用例、订正 README/S10 陈旧数字与变异条数口径；`lib/**` 未覆盖 **26→15**。
> **P2（S9 结构收敛）与 D1/D2/D4 保持待办**，按 §7-1 单独立项。
>
> **v2 修订摘要（2026-09-14，Issue #63 二次复核）**：本文初版（v1，PR #64）的**代码结论方向正确**，
> 但经**逐条实证复核**发现 3 处硬伤与若干数字偏差，已在 §10 记录并就地修正：
> 1. §3.1「6 个能力缺口符号因拿不到 `QRCode` 而不可用」**不成立**——`build()` 就返回 `QRCode`，
>    已用**外部包探针**实证全部可用（§10.2）。真正的缺口比 v1 描述**小得多**且性质不同。
> 2. §4 的「未覆盖行清单」与工具逐文件输出**对不上**（漏列 `lib/ecl.mbt`/`bitbuffer.mbt`/`svg.mbt`，
>    行号亦有偏差），已按 `moon coverage analyze -f cobertura` 实测重列（§10.3）。
> 3. §5.3 的文档计数命令**深度受限**（`docs/*.md` 漏掉 `docs/*/*.md` 与 `AGENTS.md`），
>    实测应为 WASI **29** 篇 / V40H **21** 篇（v1 记 28/18）。
>
> **v1 的下列结论经复核成立**：`module_dark` 零引用、`Module::*` 便捷构造是冗余糖、
> `ecl_default`/`SvgBuilder::new` 与既有 API 重复、`build_fixed` 与 `build` 的 `Some(mask)` 分支逐字复制、
> 文档冗余的成因分类（R1/R2/R3）与分级处置方向。**§7 执行纪律全部有效。**

---

## 0. 结论速览

| 面 | 规模 | 判定 | 处置 |
|----|------|------|------|
| 代码（本体） | 26 个 `.mbt` / **10,804 行**，其中测试 5,736 行 | **无「无效代码」意义上的死代码**；有 **8 个纯冗余符号**（~30 行） | 见 §2，P0 就地删 |
| 代码（模型缺陷） | 6 个公共符号「只被测试引用」 | **v1 判为「能力缺口」有误**：宿主经 `build()` 即可用（§10.2 实证）；真实缺口仅 1 个——**缺「空矩阵」公共构造** | 见 §3 + §10.2，补 1 个出口而非「补 6 个 API」 |
| `lib/**` 未覆盖行 | **26 行**（summary 口径；cobertura 逐行 30） | **v2 重列**：约 8 行可删/可降、13 行防御性兜底（建议保留）、8 行可达未触达（建议补负向用例）；v1 的「19+7」清单漏 3 个文件 | 见 §4、§10.3 |
| 文档 | **54 个 Markdown / 15,048 行**（含 README/AGENTS） | 信息量本身有效，**冗余在「同一结论反复陈述」与「已失效口径未标注」** | 见 §5–§6 |

**一句话**：本仓库的「无效」主要不在代码，而在**文档的重复叙述**；
代码侧真正的可清理面很小（8 个冗余符号 / ~30 行），另有一处**文档与实现脱节**（README 测试数/文件数陈旧，§10.4）
与 1 个**很窄的缺口**（无「空矩阵」公共构造，宿主无法从零自建矩阵——但这**不是** v1 所说的「拿不到 `QRCode`」）。

---

## 1. 判定方法（可复跑）

文档结论必须可机械复核，本文所有数字对应下列命令：

```bash
# ① 代码规模 / 测试规模
wc -l lib/*.mbt lib/internal/*/*.mbt cmd/*/*.mbt | tail -1
bash scripts/test-scale.sh

# ② 公共符号引用面（本文 §2/§3 的表格由该口径产出）
grep -rn "<symbol>" --include=*.mbt lib cmd | grep -v pkg.generated

# ③ 覆盖率与 lib 面未覆盖行（§4）
bash scripts/coverage.sh
COVERAGE_OUT=/tmp/cov.txt bash scripts/coverage.sh

# ④ 文档规模盘点（§5）
wc -l docs/*.md docs/*/*.md docs/*/*/*.md README.md AGENTS.md | tail -1

# ⑤ 文档互链死链（§6 的验收门槛）
bash scripts/docs-link-check.sh

# ⑥ 对外 API 契约快照（§2.3 用；*.mbti 是构建产物，勿提交）
moon info && cat lib/pkg.generated.mbti
```

> `moon check --deny-warn` **无法**发现本文 §2/§3 的问题：
> `unused_package` 只管包级 import，公有多余符号（`pub fn`）本身不告警。
> 因此「无效代码」的判定不能只靠 `--deny-warn`，必须叠加「引用面计数」与「覆盖率」两把尺子。

---

## 2. 代码侧：真正的无效 / 冗余（体量小，P0 可直接清）

### 2.1 判定口径

1. **纯冗余**：函数体与 `module.mbt` 已有的 `Module::new(v, t)` **完全等价**，仅是字面量糖；
2. **无调用方**：全仓库（含测试）零引用；
3. **零成本**：删除后 `moon test` / `moon check --deny-warn` / `.mbti` 无变化（不破坏公共契约）。

### 2.2 清单

| # | 位置 | 行数 | 判定 | 证据 | 处置 |
|:-:|------|:----:|------|------|------|
| H1 | `lib/module.mbt:101-105` `Module::alignment(Bool)` | 5 | 纯冗余：完全等价 `new(v, Alignment)` | `grep -rn "Module::alignment"` 仅 `fast_qr_moonbit_wbtest.mbt:41` | **删**（测试同步改 `Module::new(true, Alignment)`） |
| H2 | `lib/module.mbt:107-111` `Module::timing(Bool)` | 5 | 同上 | 仅 `*_wbtest.mbt:42` | **删** |
| H3 | `lib/module.mbt:113-117` `Module::format(Bool)` | 5 | 同上 | 仅 `*_wbtest.mbt:43` | **删** |
| H4 | `lib/module.mbt:119-123` `Module::version(Bool)` | 5 | 同上 | 仅 `*_wbtest.mbt:44` | **删** |
| H5 | `lib/module.mbt:131-135` `Module::empty(Bool)` | 5 | 同上 | 仅 `*_wbtest.mbt:46` | **删** |
| H6 | `lib/module.mbt:95-99` `Module::finder_pattern(Bool)` | 5 | 同上 | 仅 `*_wbtest.mbt:40` | **候选删**（见 §2.4 反向证据） |
| H7 | `lib/module.mbt:157-160` `module_dark()` | 4 | **零引用**（含测试） | `grep -rn "module_dark"` 仅声明 | **删** |
| H8 | `lib/module.mbt:163-165` `module_light()` | 3 | **仅 1 处内部调用**（`qr.mbt:42`），且语义= `false` | `grep -rn "module_light"` | 合并进 `Module::data(false)`，删函数 |

**H1–H5、H7、H8 合计约 30 行**，是本次盘点中**唯一**符合「可删且零风险」的代码。

### 2.3 为什么「保留 H6」的反向证据

`fast_qr` 侧这 7 个便捷构造是 `const fn`，**零运行时成本**，且是参考 crate 的公开面
（`src/module.rs`，逐条 `#[must_use] pub const fn`）。本仓库作为**逐位移植**，
保留同形 API 有「便于对照阅读」的收益，只是 MoonBit 无法把函数体内联为零成本常量的
*符号* 层面——它仍会出现在 `.mbti` 里。

**故给出可选的两种口径，由维护者择一（本文不预判）**：

| 口径 | 做法 | 收益 | 代价 |
|------|------|------|------|
| **A. 对齐参考（推荐）** | 保留 H6，并把它**用起来**（见 §3.1 的 `QRCode` 访问器） | 与 `fast_qr` 公共面一一对应，移植可读性最好 | `.mbti` 稍宽 |
| **B. 只留最小面** | H1–H6 全删，只留 `new` + `data` | 公共面最窄，无障碍 | 与参考不再逐项对应，后续移植比对需查表 |

> 若采 B，务必同步更新 `docs/moonbit-实现布局与文件职责.md` 与 `docs/移植参考/专有概念/module.md` 的对照表。

### 2.4 公共符号引用面全量实测（16 个「零生产引用」符号）

下表是 §3 的依据，由 §1 的 ② 口径产出（`inOut` = `cmd/*` 引用数，`inLib` = `lib` 非测试引用数）：

| 符号 | 位置 | inOut | inLib | 测试 | 归类 |
|------|------|:-----:|:-----:|:----:|------|
| `module_dark` | `lib/module.mbt:157` | 0 | 0 | 0 | **死代码**（§2.2 H7） |
| `Module::finder_pattern` | `lib/module.mbt:95` | 0 | 0 | 2 | 冗余糖（§2.2 H6） |
| `Module::{alignment,timing,format,version,empty}` | `lib/module.mbt` | 0 | 0 | 2–7 | 冗余糖（§2.2 H1–H5） |
| `QR_MAX_WIDTH` | `lib/qr.mbt:15` | 0 | 0 | 1 | 参考侧 `pub(crate)`；本仓建议降为 `let`（§3.3） |
| `ecl_default` | `lib/ecl.mbt:47` | 0 | 0 | 2 | 语义即 `ECL::Q`；建议删（§3.3） |
| `ECL::to_char` | `lib/ecl.mbt:53` | 0 | 1 | 3 | 参考 `Display` 对应物；**能力缺口**（§3.1） |
| `QRCode::for_version` | `lib/qr.mbt:48` | 0 | 0 | 5 | **能力缺口**（§3.1） |
| `QRCode::meta` | `lib/qr.mbt:126` | 0 | 0 | 5 | **能力缺口**（§3.1） |
| `QRCode::build_fixed` | `lib/qr_build.mbt:109` | 0 | 0 | 3 | **内部分层**（§3.2） |
| `Shape::from_name` | `lib/shape.mbt:42` | 0 | 0 | 8 | **能力缺口**（宿主按名选形状，§3.1） |
| `SvgBuilder::module_color` | `lib/svg.mbt:86` | 0 | 0 | 1 | **能力缺口**（README 快速开始示例却「用到」了它，§3.1） |
| `SvgBuilder::new` | `lib/svg.mbt:58` | 0 | 0 | 1 | 与 `default()` 重复；建议删 `new`（§3.3） |

**关键区分**（避免把能力缺口误判成死代码）：

- **死代码** = 语义可由已有 API 完全替代（`module_dark` / 冗余糖 / `ecl_default` / `SvgBuilder::new`）→ **可删**；
- **能力缺口** = 对外部调用者**本应可用**，但公共 API 缺了入口 → **补 API，不是删符号**。

---

## 3. 代码侧：不是死代码，而是结构性缺口（P1）

### 3.1 缺口根因（v2 修正）：**不是「拿不到 `QRCode`」，而是「拿不到空矩阵」**

> ⚠️ **v2 更正**：本节 v1 标题为「`QRCode` 无法从公共 API 获得」，**论断错误**。
> `QRBuilder::build()` / `QRCode::build()` 都是 `pub`，返回 `Result[QRCode, QRCodeError]`——
> 宿主**本来就拿得到 `QRCode`**。详见 §10.2 的外部包探针实证。

修正后的准确表述：

- `QRCode` 是 `pub(all) struct`（字段公开），且合法的读取路径 `build()` 返回它 —— **访问器本身可用**；
- 真正的缺口只有一条：**没有公开的「从零构造空/自建矩阵」入口**。
  `QRCode::default(size)` 存在但语义奇怪（带参数、全亮矩阵、元数据全 `None`），
  且 README 未把它列为对外 API；`QRCode::for_version(v)` 是它的 `pub` 包装，**同样未文档化**。
- 因此若宿主想「先自建/改写一张矩阵再输出」（如叠加自定义图案），只能改写 `qr.set(...)` 逐格写，
  起点是一张从 `build()` 得到的**已编码满矩阵**，语义错位。

**结论**：这不是「6 个符号不可用」，而是「**缺 1 个语义清晰的空矩阵构造**」。
v1 表中所列的 7 个符号中，6 个（`get/set/meta/Shape::from_name/ECL::to_char/module_color`）
经实证**均可由宿主正常调用**（§10.2）；唯一确有语义缺口的是 `for_version` 的**文档化**而非可用性：

| 能力 | 符号 | 现状 | 参考 fast_qr |
|------|------|------|--------------|
| 逐格读 | `QRCode::get` | 有，但先得有 `QRCode` | `Index<usize>` 切片 |
| 逐格改（不可变） | `QRCode::set` | 同上 | `IndexMut<usize>` |
| 元数据快照 | `QRCode::meta` | 同上 | 字段直接读（`pub`） |
| 版本构造 | `QRCode::for_version` | 同上 | `QRCode::default(size)` |
| 形状按名选 | `Shape::from_name` | 有，但 CLI/宿主无入口演示 | `From<String> for Shape` |
| ECL 显示 | `ECL::to_char` | 有，无宿主入口 | `Display for ECL` |
| SVG 配色 | `SvgBuilder::module_color` | 有，但 **README「快速开始」示例就用了它**，而 `cmd/main` 只调 `default()` | `Builder` trait |

> **README 与实现不一致**：README「快速开始」给出
> `SvgBuilder::default().module_color(...).background_color(...).shape(...)` 的完整链式示例，
> 但 `cmd/main`（唯一演示入口）只跑 `SvgBuilder::default().to_str(qr)`。
> 要么把示例落到 CLI，要么把示例改成 `default()` 口径——**两者必须一致**。

**建议（P1，单独立项）**：新增一个**只读出口**让宿主拿到可用的 `QRCode`：

```
QRCode::empty(version : Version) -> QRCode     # 公开的「空矩阵」构造（= 现 for_version，去掉 ? 元数据歧义）
QRCode::from_matrix(size, data) -> QRCode      # 可选：宿主自建矩阵（需校验 size*size <= QR_MAX_MODULES）
```

并把 `QRCode::meta` / `get` / `set` / `Version::width` / `Shape::from_name` / `ECL::to_char`
在 README 的「公共 API 一览」中**列入对外契约**，同时把上述链式示例落到 `cmd/main`。
这样 §2.4 的 6 个「能力缺口」符号会立刻变成**有生产引用**，README 与 CLI 也自动一致。

### 3.2 `QRCode::build_fixed` 的层级归属（P2）

`build_fixed`（`lib/qr_build.mbt:109`）是 `QRCode::build(mask=Some(m))` 的等价路径，
但 `build` 内部**没有调用它**——两条路径的 `encode`/`structure`/`create_fixed_qr` 三行
是**逐字复制**的。这不是死代码，但有**双份实现**风险：改一处漏一处会静默漂移。

**建议**：让 `QRCode::build` 的 `Some(m)` 分支直接委托 `build_fixed(...)`，
把 `build_fixed` 降为 `fn`（非 `pub`），或保留 `pub` 但由 `build` 复用。
判定口径：改完后 `lib/pkg.generated.mbti` 应只剩 `QRCode::build`（若降级），
`cmd/bench --dump` 三点 sha256 不变（`bash scripts/diff-gate.sh`）。

### 3.3 明确建议删除的「无信息量」符号（P0）

| 符号 | 理由 | 替代 |
|------|------|------|
| `module_dark()` | 零引用，语义 = 字面量 `true` | 直接用 `true` |
| `module_light()` | 唯一调用点的语义 = `false` | 用 `Module::data(false)` |
| `ecl_default()` | 语义 = `ECL::Q`，零生产引用 | 用 `ECL::Q`（并在 `select_capacity` 注释里点名默认值） |
| `SvgBuilder::new()` | 与 `default()` 完全等价（body 就是 `default()`） | 用 `SvgBuilder::default()` |
| `QR_MAX_WIDTH` | 零生产引用；参考侧也是 `pub(crate)` | 降为模块内 `let`（或与 `QR_MAX_MODULES` 二选一） |

> **不删**：`QRCode::get/set`、`Module::data/dark/set/toggle`、`Module::byte` 的 `pub` 身份——
> 见 §3.1，它们是能力出口，删了会让宿主彻底无法读矩阵。

---

## 4. 覆盖率视角的「无效/不可达」代码（lib 面 **26 行**）

> ⚠️ **v2 重列**：v1 的逐条清单与工具输出**对不上**（漏了 3 个文件、若干行号偏差，见 §10.3）。
> 下表由 `moon coverage analyze -- -f cobertura` 的机器可读输出实测得出（本机 `moon 0.1.20260904`）。
>
> **口径提示**：门禁 T5-b 读的是 summary 的 `lib/**` 合计 = **26**；
> 而 cobertura 按「行」计为 **30**。二者差异来自工具的**语句/行**计数口径，非错误——
> 但因此**逐条清单之和不必等于 26**，评审时应以「文件 + 行号」为准而非求和。

### 4.1 实测未覆盖行（`lib/**`，按文件）

`bash scripts/coverage.sh` 的 summary 口径：`lib/**` 未覆盖 **26 行**（门禁上限 26，未下降）。
cobertura 逐行口径（下表 `行数` 即该口径），共 30 行：

| 位置（文件:行） | 行数 | 分类 | 说明 / 建议 |
|------|:----:|------|------|
| `lib/internal/matrix/datamasking.mbt:148,150,152,153,154,174` | 6 | 不可达兜底 | `apply_mask_general` 全函数 + `:174` `_ =>` 分派。**注意**：它并非「语法上死代码」——`apply_mask` 是 internal 包的 `pub fn`，若传 `mask_idx >= 8` 仍会进入；只是**前置不变式（调用方恒传 0..=7）**使其不可达。建议删函数 + `_` 分支改 `abort` 护栏（把不变式变成机器可查） |
| `lib/internal/constants/capacity.mbt:212,228,239,255,266,276,306` | 7 | 越界夹取 | 5×「版本越界 → 39/夹取」+ `len<0` 兜底；调用方恒传合法值。建议保留或改 `abort` |
| `lib/internal/matrix/score.mbt:44,55,72,183` | 4 | 可达未触达 | 图案判定特定窗口（quiet-zone 边界形态）。**建议补负向用例**（注意 v1 把 `:183` 误记为 `:182`） |
| `lib/ecl.mbt:56,57,58` | 3 | **可达未触达（v1 漏列）** | `ECL::to_char` 的 `M`/`Q`/`H` 三臂。现有测试只断言 `to_char(ECL::L)`（`fast_qr_moonbit_wbtest.mbt:75`）。**建议补 3 臂断言**（枚举全量回环，零成本且有信息量） |
| `lib/internal/constants/hardcode.mbt:806,851,853` | 3 | 越界夹取 | 2× 越界夹取 + `percent_score` 双侧兜底；调用方恒传 0..=99 |
| `lib/svg.mbt:59` | 1 | **可达未触达（v1 漏列）** | `SvgBuilder::new()` 函数体。**与 §3.3「建议删 `SvgBuilder::new`」互为证据**：删掉即归零 |
| `lib/internal/bitstream/bitbuffer.mbt:28` | 1 | **可达未触达（v1 漏列）** | `CompactQR::new()` 的空缓冲行；库内构造走带容量路径。保留（公共构造）或加一条白盒用例 |
| `lib/internal/matrix/placement.mbt:71` | 1 | 越界防御 | 迁移遍历坐标恒在界内（v1 记 `:70-71`，实为 `:71`）。保留低成本防御 |
| `lib/module.mbt:158` | 1 | 死代码（§2.2 H7） | `module_dark` 函数体；删除后自动归零 |
| `lib/qr_build.mbt:120` | 1 | 可达未触达 | `build_fixed` 容量失败早退；建议补负向用例（`SpecifiedVersion`） |
| `lib/qr_output.mbt:20` | 1 | 不可测副作用 | `println`；注明不可测即可（S10 铁律 7） |
| `lib/internal/data_encoding/encode.mbt:191` | 1 | 负向护栏 | Alnum 前置已校验字符集；保留 |

**归类小结**：死代码/可删 **7 行**（`datamasking` 6 + `module_dark` 1，另 `svg.mbt:59` 1 行可随 `SvgBuilder::new` 删除）= 可降 **8 行**；
防御性兜底 **13 行**（`capacity` 7 + `hardcode` 3 + `placement` 1 + `encode` 1 + `bitbuffer` 1… 具体以保留为准）；
**可达未触达、建议补用例 8 行**（`score` 4 + `ecl` 3 + `qr_build` 1）。

### 4.2 处置建议（按「信息量」而非「覆盖率数字」排序）

1. **删**：`apply_mask_general` + `module_dark` + `SvgBuilder::new`（§3.3）→ 约 **8 行**归零，零风险；
2. **补**：`ECL::to_char` 全 4 臂（3 行）、`score` quiet-zone 边界（4 行）、`build_fixed` 早退（1 行）
   —— 均为**有信息量的负向/回环用例**，符合 S10 铁律 5/6；
3. **注明不可测**：`qr_output.println`（1 行），在 `docs/S10b` 正文登记理由；
4. **保留**：`capacity`/`hardcode`/`placement`/`encode` 的越界兜底（13 行）——低成本防御，删了反而脆弱。

> **与 S10 §5 铁律 5/6 一致**：优先补「能让测试变红」的负向/契约用例，**不为覆盖率数字造无信息量用例**。
> 若采纳第 1 步，`lib/**` 未覆盖行会下降，届时须同步更新 `docs/S10b-测试覆盖率报告.md`
> 顶部的 `<!-- coverage-floor: lib_uncovered=N -->` 并在正文写明理由（`bash scripts/coverage.sh --floor` 门禁要求）。

> **与 S10 §5 铁律 5/6 一致**：优先补「能让测试变红」的负向/契约用例
> （如 `score` 的 quiet-zone 边界、`build_fixed` 的 `SpecifiedVersion`），
> **不为覆盖率数字造无信息量用例**。
>
> 若采纳 §4.1 的删除，`lib/**` 未覆盖行会显著下降，
> 届时须同步更新 `docs/S10b-测试覆盖率报告.md` 顶部 `<!-- coverage-floor: lib_uncovered=N -->`
> 并在正文写明理由（`bash scripts/coverage.sh --floor` 门禁要求）。

---

## 5. 文档侧：规模盘点与冗余结构

### 5.1 盘点

| 分组 | 篇数 | 行数 | 角色 |
|------|:----:|:----:|------|
| `README.md` + `AGENTS.md` | 2 | 632 | 唯一对外口径汇总 + 协作硬约定 |
| `docs/S1–S8`（实现阶段） | 8 | 3,789 | 逐阶段实现方案/记录 |
| `docs/S9*`（性能/体积系列） | 15 | 5,736 | S9 + S9b–S9o，含 5 篇纯历史复测 |
| `docs/S10*`（测试系列） | 3 | 939 | roadmap + 覆盖率 + bug 修复记录 |
| `docs/S11*`（清理评估，v2） | 1 | ~470 | 本文：判定口径 + 处置队列 + v2 复核 |
| `docs/移植参考/**` | 12 | 1,280 | Rust 参考库分析语料 |
| `docs/` 其他（布局/工具链/环境/资产） | 11 | 2,641 | 原理与 setup |
| `.githooks` + `.codebuddy` | 3 | 31 | 钩子说明与 AI 命令提示 |
| **合计（当前）** | **56** | **~15,700** | v1 记 54/15,048（未计 S11 自身，见 §10.3） |

复算口径（NUL 分隔，避免中文文件名被转义；**必须递归，勿用 `docs/*.md`**）：

```bash
git ls-files -z '*.md' | xargs -0 wc -l | tail -1
```

> ⚠️ v1 的 **54 篇 / 15,048 行**是**不含 S11 自身**的基线；当前仓库（含 S11 v2）以实跑 `wc -l` 为准。
> 数字**只作冗余量级参考**，不参与任何门禁。

### 5.2 冗余的三类成因（不是「文档没用」，而是「结论被复述了 N 遍」）

| 类型 | 说明 | 典型 |
|------|------|------|
| **R1 跨文档复述** | 同一数字/同一结论在 README + S9x + 报告多处各写一遍，改一处要改多处 | 性能三点口径同时出现在 README §性能与体积、S9c、S9d、S9e、S9j、S9o 共 6 处 |
| **R2 历史口径未标注** | 后端已收敛为 `wasm-gc`，但 S9/S9c/S9e/S9f/S9h 仍以 3–4 后端并列叙述，读者需自行判断哪列有效 | `WASI` 字样散落在 **29 篇**（§10.3 修正；v1 记 28 系命令漏 `docs/*/*.md` 与 `AGENTS.md`） |
| **R3 过程留痕过重** | S9b/S9c/S9e/S9h/S9f/S9g 是「当时的复测/异常归因」记录，结论已并入 S9n/S9o/README，正文仍完整保留 | S9h（Node 版本与宿主漂移归因）结论已并入 S9n §1 |

### 5.3 冗余度实测线索

```bash
# 同一关键数字在几篇文档里出现（以性能主口径为例）——**注意用 `git ls-files` 避免漏层**
git ls-files -z '*.md' | xargs -0 grep -lZ "V40H" | tr '\0' '\n' | wc -l
# 已失效后端字样仍出现在几篇
git ls-files -z '*.md' | xargs -0 grep -lZ "WASI" | tr '\0' '\n' | wc -l
```

- `V40H` 出现在 **21 篇**；`WASI` 出现在 **29 篇**（多数是「已移除」的说明，但叙述密度仍高）。
- ⚠️ **v1 的 `grep -rln "V40H" docs/*.md README.md` 有深度缺陷**：通配 `docs/*.md` **不递归**，
  漏掉 `docs/移植参考/**`（12 篇）与 `AGENTS.md`，故 v1 得 18/28（且含本文自身）偏低；
  正确口径见 §10.3。**数字类结论一律用 `git ls-files -z '*.md'` + `xargs -0`**。
- 结论（不变）：**冗余不在总量，而在「同一结论的载体数量」**。

---

## 6. 文档侧：处置方案（分级，标注风险）

> **总原则**：把 `README.md` 作为**唯一对外口径**；`docs/S*` 降级为**证据与历史档案**。
> 归档≠删除；历史记录有审计价值（S10 的钉版可复现依赖它）。

### 6.1 P0：低成本、零信息损失（建议本迭代做）

| # | 动作 | 范围 | 验收 |
|:-:|------|------|------|
| D1 | **失效口径统一标注**：在仍并列多后端的 S9/S9c/S9e/S9f/S9h 顶部加一行状态 banner（「本文含已移除的 `wasm(WASI)`/`js` 口径，现行口径见 README + S9n」） | 5 篇 | `bash scripts/docs-link-check.sh` 通过 |
| D2 | **建 `docs/历史归档/README.md` 索引**，把纯历史篇（S9c/S9e/S9f/S9g/S9h）登记为「归档：结论已并入 X」 | 1 新文件 + 5 行 | README 文档索引表加一行 |
| D3 | **README 与 CLI 一致性（v2 扩充）**：① 把 README「快速开始」的 SvgBuilder 链式示例落到 `cmd/main`（或反向收敛示例）；② **顺手修正 README 陈旧数字**——`135 单测 / 24 个测试文件` 实际为 **140 / 26**（§10.4，v1 漏，属同一「README 与实现脱节」问题） | README + `cmd/main/main.mbt` | `moon run cmd/main` 输出含自定义配色；README 测试数与 `moon test` 实测一致 |
| D4 | **`docs/README优化-冗余清理与最佳实践.md` 与 `docs/S9o-…冗余清理.md` 合并**，二者主题同为「README 去冗余」，清单一分为二 | 2 → 1 | 死链检查通过 |

### 6.2 P1：结构性收敛（建议单独立项）

| # | 动作 | 收益 | 风险 |
|:-:|------|------|------|
| D5 | **S9 系列合并为 3 篇**：《S9 基准与口径》《S9b 优化与已否决项（含 S9m/S9n）》《S9c 体积（含 S9f/S9g/S9i）》；其余 10 篇移入 `docs/历史归档/S9/` | 目录从 16 → 3 + 归档，读者 1 跳到位 | 需重写交叉引用，死链风险高 → 必须 `docs-link-check.sh` + 人工抽查 |
| D6 | **数字单点化**：README 只保留「结论 + 复跑命令」，全部原始数字留在对应 S9 篇章；禁止 README 与 S9 同时给同一表的完整副本 | 改数据只改一处 | 需逐表核对，避免删掉唯一出处 |
| D7 | **阶段文档（S1–S8）不动**：它们是「实现方案/记录/评审」的三部结构，属**移植可读性资产**，合并会丢信息 | — | — |

### 6.3 P2：可选（评估后不建议贸然做）

| # | 动作 | 判断 |
|:-:|------|------|
| D8 | `docs/移植参考/**`（15 篇）合并 | **不建议**：分析对象是外部参考库，与主仓演化解耦，合并收益低 |
| D9 | `docs/moonbit-重写-roadmap-详细分析.md` / `docs/项目基础框架-详细分析.md` / `docs/core-仓库布局参考与目标架构.md` 归档 | **建议只加状态 banner**（「历史基线，现行布局见 `AGENTS.md` + `moonbit-实现布局与文件职责.md`」），不移动文件 |

---

## 7. 执行纪律（务必遵守，否则会破坏现有门禁）

1. **一次一个主题、单独 PR**：代码清理（§2/§3）与文档重构（§6）**不要混在一个 PR**，
   否则回滚代价高、评审难。
2. **删除前先跑三件套**：
   ```bash
   export PATH="$HOME/.moon/bin:$PATH"
   moon fmt && moon info && moon check --deny-warn && moon test
   bash scripts/test-scale.sh && bash scripts/docs-link-check.sh
   ```
3. **`lib/**` 改动前确认工作区干净**：`bash scripts/test-audit.sh mutation` 会
   `git checkout -- lib/` 还原，**未提交的改动会被一并清掉**（AGENTS.md §三.4 陷阱）。
4. **小步提交**：每完成一处有意义的改动即 commit（AGENTS.md §三.5）。
5. **改公共面必须同步**：
   - `lib/pkg.generated.mbti` 是构建产物（gitignore），**不要提交**；
   - API 增删须同步 README「公共 API 一览」+ `docs/moonbit-实现布局与文件职责.md`；
   - 新增/删除文档须同步 README 索引表，否则 `docs-link-check.sh` 会红。
6. **不要为了「看起来干净」删有信息量的历史**：S9b 的「已否决项」、S10 的「偏差记录」
   是**防止重复踩坑**的资产，属**保留**而非冗余。

---

## 8. 建议执行顺序（一页清单）

| 序 | 动作 | 对应章节 | 预计影响 |
|:--:|------|:--------:|----------|
| 1 | 删 `module_dark` / 合并 `module_light` / 删 `ecl_default` / 删 `SvgBuilder::new` | §2.2 H7/H8、§3.3 | ~15 行，零风险 |
| 2 | 删 `Module::{alignment,timing,format,version,empty,finder_pattern}` 或保留（二选一后统一文档口径） | §2.2 H1–H6、§2.3 | ~30 行 + 4 处测试改写 |
| 3 | 删 `apply_mask_general` 及不可达 `_` 分支；`build_fixed` 由 `build` 复用 | §3.2、§4.1 | ~15 行，覆盖率 lib 面下降 |
| 4 | 同步 `docs/S10b` 的 `coverage-floor` 标记与正文 | §4.2 | 门禁一致 |
| 5 | 补负向用例：`score` quiet-zone 边界、`build_fixed` 的 `SpecifiedVersion`、**`ECL::to_char` 全 4 臂（v2 新增，§10.3）** | §4.2 | 覆盖率 + 变异检测双重收益 |
| 6 | 文档：D1 banner + D2 归档索引 + **D3 README/CLI 一致 + 陈旧数字修正（v2 扩充）** + D4 合并 | §6.1 | 54 篇 → 53 篇 + 1 索引 |
| 7 | （立项，**v2 缩窄**）新增 1 个「空矩阵」公共构造 `QRCode::empty(version)`，并在 README 公共 API 表登记 `meta/get/set/for_version/Shape::from_name/ECL::to_char`（**注意：这些符号本就可用，本项是「文档化」不是「解封」**） | §3.1、§10.2 | 补 1 个语义出口 + 契约补全 |
| 8 | （立项）S9 系列合并 D5/D6 | §6.2 | 16 篇 → 3 篇 + 归档 |

---

## 9. 与既有文档的关系

- 本文**不替代** [S10-测试用例设计与完善roadmap.md](./S10-测试用例设计与完善roadmap.md)：
  S10 管「测试该覆盖什么」，本文管「代码/文档该留什么」。
- 本文**不替代** [S10b-测试覆盖率报告.md](./S10b-测试覆盖率报告.md)：
  覆盖率数字仍以 S10b 为准，本文 §4 解释其**成因**与**处置**。
- 布局与文件职责仍以 [moonbit-实现布局与文件职责.md](./moonbit-实现布局与文件职责.md) 为准；
  本文若与其冲突，以该文档为准并回改本文。


---

## 10. v2 复核记录：S11 初版（v1）的漏洞与修订

> 本节回应 Issue #63 的第二轮要求（「重新思考 && 评估上述方案是否存在漏洞，如何优化」）。
> **方法**：把 v1 的每条**可机械验证的论断**当作待验假设，用下列命令/探针逐一复跑；
> 环境：`moon 0.1.20260904 (94521db)`，`main@533e4cb`，`moon test` **140 全绿**。
> **结论**：v1 的**方向与纪律正确**，但有 **3 处硬伤 + 若干数字偏差**，已就地修正。逐条如下。

### 10.1 复核清单（结论一览）

| # | v1 论断 | 复核结果 | 处置 |
|:-:|---------|---------|------|
| V1 | §3.1「`QRCode` 无法从公共 API 获得」→ 7 个符号「不可用」 | ❌ **错误**：`build()` 返回 `QRCode`，7 个符号宿主全可用（§10.2 探针实证） | 改 §3.1；缺口缩为「缺空矩阵构造」1 条 |
| V2 | §3.1「`SvgBuilder::module_color` 是能力缺口」（理由：CLI 只调 `default()`） | ❌ **错误**：`module_color` 是 `pub` 方法、`default()` 是 `pub`，链式调用天然可用（探针实证） | 从「缺口」移出；CLI 不演示属 D3 文档一致性问题 |
| V3 | §3.1「`Shape::from_name` / `ECL::to_char` 是能力缺口」 | ❌ **错误**：二者均为 `pub`、无需 `QRCode`，宿主可直接调 | 同上 |
| V4 | §2.2「8 个冗余符号 / ~30 行」 | ✅ 成立（`module_dark` 零引用；5 个 `Module::*` + `finder_pattern` 仅测试；`ecl_default`/`SvgBuilder::new` 与既有 API 重复） | 保留 |
| V5 | §3.2「`build_fixed` 与 `build` 的 `Some(mask)` 分支逐字复制」 | ✅ 成立：`build_fixed` 体 `qr_build.mbt:122-134` 与 `build` 的 `Some(m)` 分支 `:159-168` 中，「`create_fixed_qr` + `wrap_packed`」两段逐字重复 | 保留 |
| V6 | §4 未覆盖行「19 不可达 + 7 可达」及逐条清单 | ⚠️ **部分错误**：总数 26 对，但清单**漏 3 个文件**、若干行号偏差（§10.3） | 重列 §4 |
| V7 | §5.3「`V40H` 18 篇 / `WASI` 28 篇」 | ⚠️ 命令深度受限导致偏低；实际 **21 / 29**（§10.3） | 改 §5.2/§5.3 |
| V8 | 通篇未提 README「135 单测 / 24 测试文件」 | ❌ **漏检**：实际 **140 / 26**（§10.4）——与 v1 自己主张的「README 与实现不一致」同类问题 | 新增 D3 ②、§10.4 |

### 10.2 硬伤 #1：`QRCode` 能力缺口论断不成立（**已实证**）

v1 §3.1 的核心论证是「仓库内无 `QRCode` 字面量构造，宿主拿不到 `QRCode`，故 6 个符号只被测试用」。
该论证**前提与结论都错**：宿主从 `QRBuilder::build()` 拿到的**就是** `QRCode`。

**复跑证据**（外部包探针，模拟真实下游消费者；探针用完即删）：

```bash
mkdir -p cmd/probe_tmp && cat > cmd/probe_tmp/moon.pkg <<'EOF'
pkgtype(kind: "executable")
import { "tryandrun/fast_qr_moonbit/lib" }
EOF
cat > cmd/probe_tmp/main.mbt <<'EOF'
fn main {
  let qr = match @lib.QRBuilder::from_string("https://example.com/").build() {
    Ok(q) => q
    Err(_) => abort("x")
  }
  let (v, e, m, md) = qr.meta()                        // QRCode::meta
  println(qr.get(0, 0).value().to_string())            // QRCode::get
  let q2 = qr.set(0, 0, @lib.Module::data(true))       // QRCode::set
  println(q2.size().to_string())
  let _ = @lib.Shape::from_name("circle")              // Shape::from_name
  let _ = @lib.ECL::to_char(@lib.ECL::L)               // ECL::to_char
  let svg = @lib.SvgBuilder::default().module_color("#0000ff")
              .background_color("#fff").shape(@lib.Shape::Circle).to_str(qr)  // module_color 链
  println("svg=" + svg.length().to_string())
  let empty = @lib.QRCode::for_version(@lib.Version::V01)   // for_version
  println("empty=" + empty.size().to_string())
  let _ = @lib.Module::alignment(true)                 // Module 便捷构造
  println("ok")
  let _ = (v, e, m, md)
}
EOF
export PATH="$HOME/.moon/bin:$PATH"
moon check cmd/probe_tmp --deny-warn   # ✅ Finished（0 警告 0 错误）
moon run   cmd/probe_tmp --target wasm-gc
```

**实测输出（本机）**：

```
meta ok
true
25
svg ok 8207
for_version ok 21
module ctor ok
```

→ `meta / get / set / Shape::from_name / ECL::to_char / module_color / for_version / Module 便捷构造`
**全部从外部包成功调用**。故 v1「6 个能力缺口」是**误判**：

- **真正的问题只有 1 个**：缺一个**语义清晰的「空矩阵」公共构造**（`default(size)` 语义怪、未文档化）；
- **其余 6 个符号的问题不是「不可用」，而是「未在 README 公共 API 表中登记」**——
  属**文档契约不全**（D3），**不是代码缺口**，不该用「补 API」解决。
- 推论：v1 §8#7「新增 `QRCode::empty/from_matrix` 以关闭 6 个缺口」**过度设计**；
  v2 缩为「新增 1 个 `QRCode::empty(version)` + 补 README 契约表」。

> **方法论教训**：v1 §1 的 ② 用 `grep` 数**符号声明处**来推「引用面」，
> 对 MoonBit 的**方法调用语法**（`qr.get(...)` 而非 `QRCode::get(...)`）会**系统性低估**；
> 正确做法是**用外部包探针编译**（本节能通过 = 可用），或 `moon info` 看 `.mbti` 契约，
> 而不是数 grep 命中。**「只被测试引用」在 grep 口径下与「宿主不可用」不等价。**

### 10.3 硬伤 #2/#3：覆盖率清单与文档计数（**命令口径缺陷**）

**(a) 覆盖率逐条对不上**：v1 §4 的清单与工具输出不一致。权威数据应取
`moon coverage analyze -- -f cobertura`（或 `-f caret`）的**行级**结果，实测（§4.1 已重列）：
`datamasking` 6 行、`capacity` 7、`score` 4、**`ecl` 3（v1 漏）**、`hardcode` 3、
**`svg:59` 1（v1 漏）**、**`bitbuffer:28` 1（v1 漏）**、`placement:71` 1、`module:158` 1、
`qr_build:120` 1、`qr_output:20` 1、`encode:191` 1。

- v1 误记 `score.mbt:182`，实为 **183**；误记 `datamasking:147-152`，实为 **148/150/152/153/154/174**。
- v1 说「19 行不可达 + 7 行可达」：修正后约「可删 8 行 + 防御 13 行 + 可补用例 8 行」，且**三者之和不必等于 26**
  （summary 与 cobertura 计数口径不同，见 §4 口径提示）。
- **漏掉的 `lib/ecl.mbt:56-58` 反而更有价值**：说明 `ECL::to_char` 只测了 `L` 臂
  ——这是一条**低成本的枚举全量回环补测点**，v1 完全没提。

**(b) 文档计数偏低**：v1 用 `grep -rln "V40H" docs/*.md README.md`，而 `docs/*.md` **不递归**，
漏 `docs/移植参考/**`（12 篇）与 `AGENTS.md`。实测：

```bash
git ls-files -z '*.md' | xargs -0 grep -lZ "V40H" | tr '\0' '\n' | wc -l   # 21（v1 记 18）
git ls-files -z '*.md' | xargs -0 grep -lZ "WASI" | tr '\0' '\n' | wc -l   # 29（v1 记 28）
```

文档**规模**（`git ls-files -z '*.md' | xargs -0 wc -l`）当前为 **55 篇 / 15,393 行**
（v1 记 54/15,048，含 v1 自身未计；见 §5.1 口径）。**结论方向不变（冗余在载体数量），数字已订正。**

### 10.4 硬伤 #4（v1 漏检）：README 与实现脱节——测试数与文件数陈旧

v1 大力主张「README 与实现必须一致」（D3），却**只盯着 SvgBuilder 示例**，漏了更硬的脱节点：

| README 处 | 文字 | 实测 | 差 |
|-----------|------|------|----|
| `README.md:6` | 「**135 单测** + 快照全绿」 | `moon test` → **140** | -5 |
| `README.md:283` | 「`moon test` **135 个用例全绿**」 | 同上 | -5 |
| `README.md:283` | 「**24 个**测试文件」 | `find lib cmd -name '*test*.mbt' \| wc -l` → **26** | -2 |

- 成因：S10 v5（`e387bb1`）新增了测试与用例，README 的「测试」小节未同步刷新。
- **这是与 D3 同类的「文档 ≠ 实现」，且 `docs-link-check` 抓不到**（数字不是链接）。
- **优化建议**：把 README 里的**易变数字**改为「**结论 + 复跑命令**」（一句话 + `moon test` / 一条 `find`），
  只保留**稳定标志数**（如「140」可写「140（`moon test` 输出为准）」）——即 v1 D6「数字单点化」的**最小可行版**，
  优先应用到 README 顶部「项目状态」与「测试」两处。

### 10.5 v1 的**合理内核**（经复核确认，不应推翻）

1. **「无效」主要在文档、代码可清理面很小**——成立；
2. **冗余三成因 R1/R2/R3** 与**分级处置 P0/P1/P2**——成立且实用；
3. **§2.2 的 8 个冗余符号**——成立（唯 `finder_pattern` 保留与否属口径选择，v1 已给 A/B 两案）；
4. **§3.2 `build_fixed` 双份实现漂移风险 + 建议 `build` 委托它**——成立，且是**本报告最有价值的代码侧发现之一**；
5. **§7 执行纪律（一次一主题、删除前三件套、`mutation` 会 `git checkout -- lib/`、小步提交、`.mbti` 不提交）**——全部有效，**v2 原文保留**；
6. **「不为覆盖率数字造用例」**——成立，且 v2 的补测建议（`ECL::to_char` 全臂、`score` 边界）均符合此纪律。

### 10.6 v2 优化后的处置队列（替代 §6/§8 的差额部分）

| 优先级 | 动作 | 与 v1 的差异 |
|:------:|------|--------------|
| P0-1 | 删 `module_dark` / 合并 `module_light` / 删 `ecl_default` / 删 `SvgBuilder::new`（后者还能消 1 行未覆盖） | 同 v1 |
| P0-2 | **修正 README 陈旧数字（140/26）**，并把易变数字改为「结论 + 复跑命令」 | **v2 新增**（§10.4） |
| P0-3 | `build` 的 `Some(m)` 分支委托 `build_fixed`，消除双份实现 | 同 v1 §3.2 |
| P1-1 | 删 `apply_mask_general`；`_` 分支改 `abort` 护栏（把「0..=7」不变式机器化） | 同 v1，措辞去「永远不可达」 |
| P1-2 | 补负向/回环用例：`ECL::to_char` 全 4 臂、`score` quiet-zone、`build_fixed` 早退 | **v2 增 `to_char`**（§10.3） |
| P1-3 | 新增 `QRCode::empty(version)` 1 个出口 + README 公共 API 表补登记 6 个符号 | **v2 大幅缩窄**（不再「补 6 个 API」） |
| P2 | S9 系列收敛、D1 banner、D2 归档索引、D4 合并 | 同 v1 |

### 10.7 一句话总结本轮

> v1 的**框架可复用**，但它把「**grep 数不到宿主引用**」误读成「**宿主不可用**」，
> 从而把一个**文档契约问题**（6 个符号未登记）误判成**代码缺口**（补 6 个 API），
> 并漏掉了 3 个未覆盖文件与 README 的陈旧数字。
> **v2 的净修正**：代码侧可清理面**变小且更准**（8 个冗余符号 + 1 个双份实现 + 1 个空矩阵出口）；
> 文档侧的**优先项前移**（先修 README 数字，再做结构收敛）。

---

## 11. v3 落地记录：P0/P1 实做（2026-09-14，实跑）

> 本节响应 Issue #63 第三轮：「阅读代码 && 文档 → 依次优化代码、修复 bug && 生成/更新文档」。
> **本轮只做 §10.6 队列里的 P0/P1，P2（S9 结构收敛）仍单独立项**，遵守 §7-1「一次一个主题」——
> 代码清理与文档重构**分 PR**（本 PR 为**代码清理 + 契约/数字订正**，S9 重排另起）。
> 全程门禁：`fmt --check` / `check --deny-warn` / `moon test` **146**（default + wasm-gc）/
> `wasm-gc` release 构建 / `test-scale` / `docs-link-check` **487 链零死链** /
> `coverage --floor` **15 ≤ 15** / 变异检测 **21/21 全检出**。

### 11.1 交付清单（对照 §10.6）

| 序 | 动作 | 落地 | 证据 |
|:--:|------|------|------|
| P0-1 | 删冗余符号 | `module_dark`/`module_light`/`ecl_default`/`SvgBuilder::new` + `Module::{finder_pattern,alignment,timing,format,version,empty}` 六个等价糖 | `moon check --deny-warn` 干净；`grep` 零残留引用 |
| P0-2 | README 陈旧数字 + 契约 | 测试数 **140→146**、文件数 **26**；公共 API 表补登记 `empty/for_version/get/set/meta/data/build_fixed/select_capacity/Shape::from_name/ECL::to_char`；新增「矩阵读写与自建」小节 | `moon test` 实测 146；README 与 CLI 示例一致 |
| P0-3 | 消双份实现 | `build` 的 `Some(m)` 分支**委托** `build_fixed` | m1 固定 mask 快照（对参考 hex）+ `qr_builder_chain_equals_build_fixed` 全绿 |
| P1-1 | 删不可达兜底 | 删 `apply_mask_general`，`_` 分支改 `abort` 护栏（单行） | 变异 M1（掩码 2/3 互换）仍检出 |
| P1-2 | 补负向/回环用例 | `ECL::to_char` 全 4 臂、`QRCode::empty` 契约 + 别名等价、`build_fixed` 两路负向早退、N3 quiet-zone 双侧、N1 非 Data 结算 | `moon test` 140 → **146** |
| P1-3 | 空矩阵出口 | `QRCode::empty(version)` 新增；`for_version` 降为 delegating 别名 | 黑盒 `public_qrcode_empty` |
| P2 | S9 结构收敛 / D1 banner / D2 归档 / D4 合并 | **未做**（单独立项，避免与代码清理混 PR） | 见 §6.1/§6.2 |

### 11.2 额外发现与订正（本轮实证新增）

1. **`line_score` 是第二处双份实现**（§3.2 的同类，v1/v2 均未识别）：
   `lib/internal/matrix/score.mbt` 的 `line_score`（回调取数）与生产路径 `line_scan_buf`
   （直接索引）逐位等价，且**只被白盒 helper `test_line_run`/`test_line_pattern` 调用**。
   处置：`line_score` 改为物化缓冲后**委托 `line_scan_buf`**——既消副本（覆盖 score 4 行归零），
   又让白盒断言测的**就是生产算法**（缓解 §S10 铁律 3「actual == actual」隐患）。
2. **变异条数「23/23」是错误的**（S10 附录 E/H）：该数系**误把最大编号当条数**——
   脚本实际 **21 条**（M3/M10 缺号）。已订正为 **21/21**（v4 为 19/19），
   并写入权威口径 `bash scripts/test-audit.sh mutation | grep -c 已检出`。
   → **与 §10.2 是同一类错误的两个实例**（用声明/编号推规模 → 系统性高估）。
3. **覆盖率门禁随清理下调**：`lib/**` 未覆盖 **26 → 15**，`docs/S10b` 的
   `<!-- coverage-floor: lib_uncovered=15 -->` 已同步并写明理由（§6.1 D3/§4.2 要求）。

### 11.3 剩余未覆盖 15 行的定性（刻意保留）

`bitbuffer:28` / `capacity:7` / `hardcode:3` / `encode:191` / `datamasking`(abort 护栏) /
`placement:71` / `qr_output:20` —— 均为**防御性兜底或不可测副作用**（§4.1 第 4 类）。
**不再为覆盖率数字造无信息量用例**（S10 §5 铁律 6）。

### 11.4 一句话总结

> **P0/P1 全部落地，门禁全绿，变异 21/21；`lib/**` 未覆盖 26→15。**
> 净结果：删 8 个冗余符号 + 消 **2 处**双份实现（`build`↔`build_fixed`、`line_score`↔`line_scan_buf`）
> + 补 1 个空矩阵出口 + 6 类负向/回环用例 + README/S10 数字与契约订正。
> **P2（S9 收敛）与 D1/D2/D4 保持待办**，按 §7-1 单独立项。
