# S12 — 文档体系 SDD 诊断与优化方案（可读性 × 可维护性）

> **状态**：现行　｜　日期：2026-09-18　｜　索引：[docs/README-导航与索引.md](README-导航与索引.md) §6

> 本文回应 ISSUE #78：「阅读代码 && 文档；当前 `docs/` 目录下存在大量文档，可读性极差；
> 调研网络文档 SDD 最佳实践，评估如何优化 `docs/` 的可读性 && 可维护性，详细分析；
> 汇总上述过程，生成/更新文档」。
>
> 定位：**诊断报告 + 规范提案 + 分步队列**。不是「一次大扫除的执行单」——
> 任何文件移动/合并/删除都按 §9 顺序**单独立项、单独 PR**（与 [S11](./S11-无效代码与冗余文档清理评估.md) §7 同纪律）。
>
> 日期：2026-09-18　｜　基线：`main@28f06fb`　｜　工具链：`moon 0.1.20260915`、`wasm-gc`
> 实跑门禁：`moon test` **147 全绿**（default + `wasm-gc`）、`docs-link-check` 65 文件 / 787 相对链接 **零死链**、
> `test-scale` 全绿、`publish-check` 5 项全绿。
>
> **与 S11 的分工**：S11 管「**代码/文档该留什么**」（无效代码与冗余文档的判定与清理）；
> 本文管「**留下的文档该长什么样、放哪里、凭什么准入**」（信息架构与规范）。
> 两者共享同一套「可复跑判定」纪律（§3 的每条数据都附命令）。

---

## 0. 结论速览（先看这 8 行）

| 维度 | 现状（实跑） | 判定 | 处置 |
|------|-------------|------|------|
| **规模** | `docs/` **61 篇 / 17,940 行**（含 `README.md`/`AGENTS.md` 共 63 篇 / 18,465 行） | 对 10.8k 行代码的库项目**偏重**，但**不是**「无内容冗余」 | 不删信息，**改信息架构**（§5） |
| **可读性** | 顶层 `docs/` **47 篇平铺**、**35 篇用 `S9x` 序号名**、全库 **2,052 个标题 / 204 个 H1**、最深层级 **H4** | ❌ 首次到访者无法「3 秒定位」；序号名**无语义** | 目录分类 + 命名去序号化（D2/D3） |
| **可维护性** | **同一 H2 标题在 12 篇重复**（`实现方案`/`实现记录`）、**27 篇含 ⚠️ 而无显式状态标签**、计数在 **13 篇**里各写一遍 | ❌ 同一结论 N 处载体；改一处必漂移 | 状态字段 + 数字单点化（D1/D4） |
| **SDD 缺口** | 实现规格（S1–S9）**有**方案/记录/评审三段；`docs/` **无**单一事实源清单、**无**文档准入 CheckList、**无**防漂移门禁 | ⚠️ 有 SDD 的**形**，缺 SDD 的**闸门** | 新增 §7 规范 + D1/D4 门禁 |
| **归档面** | `.moonignore` 已排除 `/docs/`；`publish-check` ⑤ 已锁「归档内不得引用归档外」 | ✅ 发布面链路**已闭环** | 不动（本文只做仓库内导航面） |
| **可自动化项** | 死链、测试文件规模、覆盖率地板、发布面链接**已有门禁** | ✅ 4 项已有 | **补** 数字漂移与状态字段门禁（D1/D4） |
| **本轮不做** | 不移动任何文件、不合并任何篇章、不改任何代码 | — | 本文是**规范与队列**，落地另开 PR |
| **净建议** | 建 `docs/` 分类目录 + 文档状态头 + 数字单点化 + 两项新门禁 | — | 见 §9 分步队列 |

**一句话**：本仓库文档的问题**不是「写了没用的东西」，而是「有用的东西没有信息架构」**——
17,940 行里几乎没有一句话该删，但它们被**平铺在一个目录、用无语义的序号命名、以 12 份重复的标题结构**呈现，
且**没有准入规范与防漂移门禁**。SDD 的处方恰是针对这一点（§2/§7）。

---

## 1. 方法与口径（可复跑）

本文所有数字由以下命令产出（**递归，勿用 `docs/*.md`**——中文文件名与子目录都会漏）：

```bash
# ① 规模
git ls-files '*.md' | wc -l
git ls-files -z '*.md' | xargs -0 wc -l | tail -1

# ② 结构：标题统计（H1..H4）与重复 H2
git ls-files -z 'docs/*.md' | xargs -0 grep -hE '^#{1,4} ' | awk '{print length($1)}' | sort | uniq -c
git ls-files -z 'docs/*.md' | xargs -0 grep -h '^## ' | sort | uniq -c | sort -rn | head

# ③ 状态字段覆盖
git ls-files -z '*.md' | xargs -0 grep -lZ '⚠️' | tr '\0' '\n' | wc -l

# ④ 数字漂移（同一计数出现篇数）
for n in 146 147; do printf '%s: ' "$n"; git ls-files -z '*.md' | xargs -0 grep -lZ "$n" | tr '\0' '\n' | wc -l; done
export PATH="$HOME/.moon/bin:$PATH"; moon test | tail -1

# ⑤ 门禁现状（4 项）
bash scripts/docs-link-check.sh && bash scripts/test-scale.sh && bash scripts/publish-check.sh
```

**口径纪律**（与 [S11 §1](./S11-无效代码与冗余文档清理评估.md) 一致）：
数字**只作量级参考**，不参与任何门禁；判定结论必须能由上面命令**机械复核**。

---

## 2. SDD 最佳实践调研（外部一手来源 + 本项目适配判定）

### 2.1 采样来源与结论

| # | 来源 | 核心主张 | 对本项目的适用性判定 |
|:-:|------|---------|---------------------|
| 1 | **GitHub Spec Kit**（`spec-driven.md`、`README.md`，2026 实读） | ① 「**权力反转**」：规格是主产物，代码是其表达（`specs` 驱动而非代码驱动）；② 五阶段工作流 `constitution → specify → plan → tasks → implement`；③ **规格必须可执行/无歧义**——否则无法生成可用系统；④ `[NEEDS CLARIFICATION]` 标记**禁止猜测**；⑤「宪法」九条含 **Simplicity / Anti-Abstraction / Integration-First**，并在 `plan` 阶段设 **Phase -1 Gates**；⑥ 模板强制**层级化细节**（主文档保持高层可读，细节外置到 `implementation-details/`） | **强适用**：⑤⑥ 可直接映射为「文档宪法 + 规模门禁 + 细节外置」；①③ 与本仓库已有的「README `mbt check` 文档测试」「门禁即契约」同源；④ 是本文 §7 状态字段的来源 |
| 2 | **Spec Kit 的收敛动作**（`implement → converge`，报告 `Converged`） | 实施后必须**回到规格做一致性收敛**，而非假设一次成功 | **强适用**：本仓库当前缺的正是「文档与实现的一致性收敛」——S11 的多次 v1→v2 复核就是手工版 converge，应机制化 |
| 3 | **Diátaxis**（文档四象限：Tutorial / How-to / Reference / Explanation） | 文档类型**不可混写**：一篇文档要么是「学习导向」要么是「任务导向」要么是「信息导向」要么是「理解导向」；混写是文档「感觉乱」的头号成因；四类各有自己的信息需求与校验方式 | **较强适用**：本仓库 `S*` 把「解释（为什么这么写）」与「参考（数字/表格）」写在同一篇（如 S9c 同时含结论表与实验设计），**符合典型混写症状**；但**不宜照搬四分类**（见 §2.2 边界） |
| 4 | 通用文档工程实践（README 作为落地页、计数不进正文、ADR/状态头、单文档规模上限、索引不复制目录） | **可发现性 > 完整性**：索引给「读者路径」，全量清单交给专门页；计数与版本号**必然漂移**，只留「结论 + 复跑命令」 | **已部分采纳**：`README优化-冗余清理与最佳实践` §6–§9 已把 README 压成落地页、计数清零；**未采纳到 `docs/` 内部** |

> **调研纪律**：以上为外部一手材料（Spec Kit 仓库原文、Diátaxis 站点）读后提炼，
> **不引用二手转述**；每条都给出「对本项目的适用性判定」，不做「照抄最佳实践」。
> 外部材料无版本锚点时（如 Spec Kit 快照），一律标注「2026 实读」，避免把它当成稳定契约。

### 2.2 本项目的边界：什么**不**适配（诚实声明）

1. **不引入 `specs/NNN-feature/` 目录与新编号体系**：本仓库的「spec 单元」是
   **模块级实现阶段（S1–S9）**，而非「按功能分支编号的需求」。引入第二套编号会与
   `S*` 系列、`scripts/`、`docs/移植参考/` 形成**三套坐标系**，净增认知成本。
2. **不做「规格生成代码」**：本仓库是**逐位对齐 Rust 参考库的移植**，
   真相来源是 `fast_qr` 钉版（`53e8c99`）+ 黄金值脚本，**不是**自然语言规格。
   强行让规格成为「生成源」会与「黄金值必须来自钉版」的 S10 铁律 1 **直接冲突**。
3. **不照搬 Diátaxis 四分类到每个文件**：本仓库已有事实标准——**S* 三段结构**
   （`实现方案` / `实现记录` / `评审与优化`）。四分类**作为「判别尺」**用于
   诊断混写（§5.2），但**不重命名文件为 tutorial/how-to/reference/explanation**。
4. **不追求「目录篇数最小」**：S11 §6.3 已判定 `docs/移植参考/**`（12 篇）**不建议合并**
   （分析对象是与主仓演化解耦的外部参考库）。本文继承该结论，**不纳入收敛对象**。

> 一句话：**取 SDD 的「闸门与信息架构」，不取它的「工作流重命名」**。

---

## 3. 现状诊断（每条附实测数据）

### 3.1 规模分布：不是「大量无效」，而是「大量证据未分层」

| 类别 | 篇数 | 行数 | 占比 | 判定 |
|------|:----:|-----:|-----:|------|
| **E 性能/体积证据**（S9 系列 18 篇） | 18 | 6,504 | 35.2% | 占比最高，但**支撑对外性能声明**，属必要证据；问题在**呈现**（§5.2） |
| **D 实现规格**（S1–S8） | 8 | 3,813 | 20.6% | 结构最规范（三段式），**应作为全库模板** |
| **H 工程/发布/其他**（布局、工具链、发布、环境） | 14 | 3,532 | 19.1% | 混装：**规范类**（布局/目录/工具链）与**过程类**（发布核验/环境 setup）同层 |
| **C 外部参考语料**（`移植参考/`） | 12 | 1,292 | 7.0% | 与主仓解耦，**自成一域**（已有独立目录与索引 ✅） |
| **F 测试证据**（S10 系列） | 3 | 981 | 5.3% | 门禁直接消费，**保留** |
| **G 清理审计**（S11 系列） | 2 | 911 | 4.9% | 判定口径有效，**保留** |
| **B 导航/元**（索引、README 优化、示例资产） | 4 | 903 | 4.9% | 只有 1 篇是真索引，另 3 篇是**元文档**混在 `docs/` 顶层 |
| **L0 落地页/协作约定** | 2 | 529 | 2.9% | 已在正确位置 |
| **合计** | **63** | **18,465** | 100% | 行数含 `README.md` 与 `AGENTS.md` |

> **读法**：**没有一个类别的行数是「无内容」的**。可读性问题在**分类缺失**——
> 47 篇文档平铺在 `docs/` 顶层，读者看不到上表这 8 个类别，只看得到 `docs/` 的字母序排列。

### 3.2 结构：标题层级与命名

实跑统计：

| 指标 | 实测 | 问题 |
|------|------|------|
| H1 总数 | **204** | `README.mbt.md` 是符号链接（与 `README.md` 同内容），统计口径需去重；**173 篇实体文件**却有 204 个 H1，说明**部分文档内有多个 H1**（三段式合并残留） |
| H2 / H3 / H4 | **404 / 743 / 298** | 层级用到 H4，**H3 数量接近 H2 的 2 倍**——说明「议题」被压在第三层，目录树天然膨胀 |
| 顶层 `docs/` 篇数 | **47** | 无分组：`S9…`、`mooncakes-…`、`rust-…`、`wasm-…`、`core-…` 混排 |
| `S9x` 序号命名篇数 | **35 / 63** | 序号**无语义**：新读者无法从 `S9n` 推断「这是优化优先级清单」 |
| 最深 `docs/` 子目录 | `docs/移植参考/专有概念/`（2 层） | 仅 `移植参考/` 有分类；其余**全部平铺** |

**重复 H2 标题实测（Top）**：

```
12x  实现方案        ← S1 S2 S3 S4 S5 S6 S7 S8 S9 S9b S9c S9d
12x  实现记录        ← 同上
12x  参考
 8x  结论先行        ← S9j S9k S9l S9m S9n S9o S9g S9i
 5x  评审与优化      ← S1 S2 S3 S5 S6
```

> **这 12 份同名 H2 不是错误，而是「三段式」的必然结果**——
> 问题在于**它只存在于每篇文件内部，全库没有一份「三段式是什么」的显式规范**，
> 于是新篇章（S9g–S9r、S12）各自发明了新结构（`结论先行`/`明确不做`/`验收与复现`），
> 形成**3 套并行的段落词汇表**。这是可维护性的核心成本。

### 3.3 可维护性：同一结论的载体数量

| 漂移对象 | 实测 | 判定 |
|----------|------|------|
| 测试用例计数 | `146` 出现在 **11 篇**、`147` 出现在 **13 篇**，而 `moon test` 实测 **147** | ⚠️ **已发生漂移**：`146` 是 v3 时点的历史值（S10/S11/S11b 仍把它写成「当前基线」） |
| `WASI`（已移除后端）字样 | **30 篇** | 多数带「已移除」注记，但叙述密度高（S11 §5.2 R2 同结论） |
| `wasm-gc` 字样 | **42 篇** | 与上条并存 = 读者需自行判断「哪列有效」 |
| `口径` 一词 | **36 篇** | —— |
| `0.1.0`（版本号） | **9 篇** | 口径见 [README优化 §9.2](./README优化-冗余清理与最佳实践.md#9-第六轮2026-09-14-v6发布面可达性闭环链接策略--发布状态--门禁-)（「不写死线上版本」）+ [S11 §10.4](./S11-无效代码与冗余文档清理评估.md)（易变数字改「结论 + 复跑命令」），但**仅在 README 落实**，`docs/` 内仍有 9 处 |

**已有缓解措施（有效但不成体系）**：

| 措施 | 覆盖 | 问题 |
|------|:----:|------|
| 头部 `⚠️ 历史记录` banner | **18 篇** | 位置、措辞、是否含「现行口径见 X」**逐篇不同**；另 **27 篇含 ⚠️ 但无状态标签**（⚠️ 被当作普通提醒用） |
| `日期：` 头部 | 约 **40 篇** | 格式基本统一 ✅，但**没有「状态」字段**（历史/现行/归档） |
| 死链门禁 | 全库 | ✅ 唯一已机制化的文档门禁 |
| 数字门禁 | **无** | ❌ 所以 `146/147` 漂移只能靠人眼发现（S11 §10.4 手工抓过一次，如今又漂一次） |

> **这是本文最重要的一条实证**：**「146 → 147」的二次漂移证明「靠人巡检」不可持续**
> （S11b §13 才巡检过口径，两轮后同一类问题复发）。SDD 的答案是**把收敛变成门禁**（§7.4 / D4）。
>
> **✅ 已处置（2026-09-18，见 §12）**：上表 3 条已收敛——
> ① 测试计数单点化到 `S10b` 顶部 `<!-- test-count: N -->`，其余篇章改写「见登记处 / v3–v5 时点数」；
> ② `0.1.0` 的「发布状态」写死已从 `README.md` 移除（改为「已发布（首个版本）」）；
> ③ 新增门禁 ② / ②b 使两类漂移**在提交前即红**，不再依赖人眼。
> （`WASI` / `wasm-gc` / `口径` 的叙述密度是**历史留痕**，不设为门禁——史料不是漂移。）

### 3.4 可读性：读者路径的实测

顶层 `docs/README-导航与索引.md`（189 行，58 条交叉引用，**密度 30.7 条/百行，全库最高**）
已经做得很好——**但它默认读者已经知道自己在找什么**。缺的是**「第一次打开 `docs/` 的人」的入口**：

- 打开 `docs/` 看到的是 **47 个文件名的字母序**（`README-导航与索引.md` → `README优化…` → `S10…`）；
- 其中 **35 个以 `S<数字>` 开头**——对新人而言这是**内部代号**，不是标题；
- `docs/` 下**没有 `README.md`**（唯一的「入口」文件名被挪到 `README-导航与索引.md`，
  在目录列表里排在 `README优化-…` 之前，**不等于目录首页**）。

> 归纳成一句：**导航面存在，但「第一跳」缺失**。

### 3.5 发布面（现状良好，列为约束以免优化时误伤）

实跑 `publish-check` 全绿：归档 34 项、`⑤ 发布面链接可达性` 通过。
**约束**（见 §7.2 规范）：`docs/**` 与 `AGENTS.md` **不在归档内**，
故 README 指向它们**必须用仓库绝对链接**；`docs/` 内部**一律用相对链接**。
**本文任何目录调整都必须保持这两条**（`publish-check` ⑤ 与 `docs-link-check` 双门禁会立刻抓到违例）。

---

## 4. 问题归因（五类根因，逐条对应处置）

| # | 根因 | 症状（实测） | 处置 |
|:-:|------|--------------|------|
| **P1** | **信息架构缺失**：`docs/` 无分类层级，47 篇平铺 | 新人无法 3 秒定位；类目不可见（§3.1 表） | **D2 目录分类** + **D3 命名去序号化** |
| **P2** | **无文档准入规范**：新篇章结构自由发挥 | 3 套并行段落词汇表（§3.2）；27 篇状态字段缺失（§3.3） | **§7 规范** + **D1 状态字段** |
| **P3** | **数字/口径多载体**：计数与口径在 10+ 篇各写一遍 | `146/147` 已二次漂移（§3.3）；`0.1.0` 9 处 | **D4 数字单点化 + 漂移门禁** |
| **P4** | **导航与内容耦合**：索引自动更新靠人工记 | 新增文档易漏更新索引（现由 `docs-link-check` 兜住「文件是否存在」，但**兜不住「是否入索引」**） | **D1 头部模板含索引字段** + **D5 索引覆盖门禁** |
| **P5** | **收敛靠人眼**：无「文档 ↔ 实现」一致性检查 | S11b §13 巡检后两轮又漂 | **D4 门禁化**（本文最重要的一条） |

---

## 5. 最佳实践映射：SDD → 本仓库（逐条落点）

### 5.1 「宪法 / 门禁」映射（Spec Kit ⑤⑥ → 本项目）

| SDD 主张 | 本仓库现状 | 落点 |
|----------|-----------|------|
| **Simplicity Gate**（禁止未来式设计） | S11 §6.3 已有「不合并外部语料」判断 | 写成 §7.1 规范第 4 条 |
| **Anti-Abstraction**（不包装已有能力） | `docs/` 无「索引的索引」✅ | 保持，禁止新增多层导航 |
| **层级化细节**（主文档高层，细节外置） | `S9r` 已作为 README 的「明细承接」✅ | **推广到 `docs/` 内部**：每篇≤800 行，超出拆「承接篇」 |
| **Phase -1 Gates**（进入实现前过闸） | `docs/` 无准入闸 | **§7.1 准入 CheckList**（新增文档必填 6 项） |
| **converge**（实施后回规格收敛） | 靠人工巡检（§3.3 已证失效） | **D4 门禁**（`docs-consistency.sh`） |
| `[NEEDS CLARIFICATION]`（不猜测） | 无对应物 | **§7.1 状态字段 `状态：`** 强制显式（`现行`/`历史`/`已失效`） |

### 5.2 Diátaxis 用作**判别尺**（不重构命名）

用四象限对现有篇章做**混写诊断**（抽样，逐篇可复核）：

| 篇章 | 主要类型 | 混写症状 | 建议 |
|------|---------|---------|------|
| `S1–S8`（实现规格） | Explanation + Reference | 三段式天然分离 ✅ | **保持为模板** |
| `S9c/S9e/S9f`（性能对比） | Reference + Explanation | 「结论表（参考）」与「为什么这么测（解释）」交错 | 结论表前移置顶，实验设计下沉（**不新开文件**） |
| `S9p/S9q`（口径与统计） | Explanation | 纯解释，结构清晰 ✅ | 保持 |
| `S10`（测试 roadmap） | How-to + Explanation | 铁律（规范）与覆盖矩阵（参考）同篇，776 行 | 已接近 800 行上限，**下次增量必拆** |
| `README优化-…`（9 轮总账） | Explanation（过程留痕） | 527 行、9 个平级 `## N.` 轮次 | **结构可读但已饱和**：新轮次应改为「索引行 + 承接篇」 |

> **Diátaxis 的正向价值**：它给出了「为什么读者觉得乱」的**可解释理由**——
> 不是内容多，而是**同一篇里同时要求读者「学」和「查」**。
> 本文据此把处置限定为**「结论前移 + 细节外置」**，而**不**改名、不拆文件。

### 5.3 「规格即契约」的映射（本项目已有，值得显式化）

本仓库实际已经实现了 SDD 最激进的一条——**可执行规格**：

- `README.md` 的 `` ```mbt check `` 块是**文档测试**，`moon test` 真编译真运行（改 API 不改示例即红）；
- 黄金值必须来自**钉版参考**（S10 铁律 1），`scripts/gen-goldens.sh --verify` 可校验未漂移；
- `diff-gate.sh` 与参考 wasm 逐位 sha256。

> **映射结论**：本仓库的「规格」是 **`.mbt` 源码 + `README.mbt.md` 文档测试 + `scripts/` 生成器**，
> **而 `docs/` 是它们的证据链与决策记录**。
> 因此 `docs/` 的准入条件应是「**能否指向一个可复跑的判定**」（§7.1 第 3 条），
> 而不是「是否写得详尽」。这条是本文与 S11「信息量」判据互补的第二把尺子。

---

## 6. 优化方案（D1–D7，按风险分级）

> **统一纪律**：一次一个主题、单独 PR；改完必跑 §1 ⑤ 的四项门禁；
> 移动文件必须同步 `docs/README-导航与索引.md` + `README.md` 索引（死链零容忍）。

### 6.1 P0：零文件移动、零信息损失（可直接做）

#### **D1 · 文档状态头 + 准入 CheckList**

给 `docs/**` 的 Markdown 增加**统一头部三字段**（Human 可读、机器可解析）：

```markdown
> 状态：现行 | 历史 | 已失效        ← 必填，取一
> 日期：YYYY-MM-DD（末次实质修订）   ← 必填（格式已有，仅统一）
> 索引：[docs/README-导航与索引.md](./README-导航与索引.md) §N   ← 必填，指明在索引中的归属
```

- **`状态` 三值语义**（取代当前 18 篇自由措辞的 `⚠️ 历史记录`）：
  - `现行`：结论仍是对外口径，可直接引用；
  - `历史`：结论已并入某现行篇（**必须写 `并入：[篇名]`**），本文仅留过程价值；
  - `已失效`：所述前提已不存在（如 `wasm`(WASI) 后端移除、旧模块名），**不得被引用为口径**。
- **准入 CheckList（新增文档必过）**：
  1. 头部三字段齐备；
  2. 单文件 ≤800 行（AGENTS §四已定，此处纳入准入）；
  3. **每条结论能指向一个可复跑判定**（命令 / 门禁 / 生成器），否则降级为「讨论稿」不入 `docs/`；
  4. 若与既有篇结论重复 → **改既有篇**，不新开（反 R1）；
  5. 同步更新两处索引（`docs/README-导航与索引.md` + `README.md`）；
  6. `bash scripts/docs-link-check.sh` 零死链。

#### **D2 · 目录分类（只做「归类」，不做合并）**

把顶层 47 篇按**读者意图**归入 6 个子目录（**文件内容一字不改**，只换路径 + 全库改链接）：

```
docs/
├── README.md                      ← 新：目录首页（由现 README-导航与索引.md 改名/前移）
├── 01-规格/                        ← 读者：我要改代码
│   ├── S1..S9（实现规格与决策记录）
│   └── 工程约定（布局/目录/工具链）
├── 02-证据/                        ← 读者：我要审计数据
│   ├── S9*（性能/体积）
│   ├── S10*（测试）
│   └── S11*（清理审计）
├── 03-过程/                        ← 读者：我要复盘决策
│   ├── 历史基线（项目框架/roadmap/core 布局/wasm 编译）
│   └── 发布过程（mooncakes 发布/阻塞项/模块名迁移）
├── 04-元/                          ← 读者：我要改文档/README
│   ├── README 优化总账
│   ├── README 示例资产
│   └── S12（本文：文档体系规范）
└── 移植参考/                        ← 保持现状（外部语料，自成一域）
```

- **为什么用数字前缀**：`docs/` 在平台/编辑器中按字典序展示，数字前缀让**意图顺序**
  在文件列表里直接可见（`01-规格` 先于 `04-元`），且**不依赖平台渲染**。
- **为什么只归 6 类**：类目越多，新增文档越难归类（Spec Kit 的 Simplicity Gate）。
- **风险**：**这是唯一高改动量项**（47 篇路径 + 全库 ~600 条相对链接）——
  必须由 `docs-link-check.sh` 全量兜底，且**单独 PR、不夹带任何内容修改**（S11 §7-1）。

#### **D3 · 命名去序号化（别名优先，改名为辅）**

`S9n`、`S9r` 这类**内部代号**对读者零信息。两条路（可叠加）：

| 路线 | 做法 | 成本 |
|------|------|------|
| **A. 标题化（推荐先做）** | 文件名保留 `S9x` 以兼容既有 600 条引用，但**标题与索引表一律用语义名**（如 `S9n · 优化优先级与 wasm-gc 收敛审计`） | 低（只改标题/索引） |
| **B. 真改名** | 文件改为 `性能-优化优先级与收敛审计.md`，`S9n` 仅作尾部标记 | 高（须全库改链 + 更新 S 系列编号说明） |

> **建议**：本轮只做 **A**。理由：编号是本仓库**实现顺序的可追溯资产**（S1→S12 记录了移植史），
> 去掉编号会丢失「阶段可追溯性」这一 SDD 正向价值。**读者友好靠标题与索引，不靠文件名**。

### 6.2 P1：结构性收敛（需单独立项）

#### **D4 · 数字单点化 + 漂移门禁（本文最重要）**

1. **单点化**：每个易变计数/口径在 `docs/` 内**只允许一个「登记处」**——
   候选登记处：测试数 → `S10b`；性能三点 → `S9p`；体积 → `S9i`；版本号 → `README.md`（已定「不写死」）。
   其余篇章**一律写「见登记处」**，不复制数字。
2. **门禁化**：新增 `scripts/docs-consistency.sh`（秒级、离线）做三件事：
   - **① 状态字段**：`docs/**/*.md` 每篇头部须含 `> 状态：`，值域 `现行|历史|已失效`；
     `历史` 须带 `并入：`；`已失效` **不得**被其它现行篇当作现行口径引用；
   - **② 计数漂移**：从 `moon test` 实跑取 `Total tests: N`，与 `docs/` 内
     **标记为「登记处」** 的篇章值比对；不一致即红（其余篇章的非登记数字**不设为门禁**，避免噪音）；
   - **③ 索引覆盖**：`docs/**` 每篇必须在 `docs/README-导航与索引.md` 中出现（反 P4 漏更新）。
3. **接入**：加入 `scripts/gates.sh` 的 `DEFAULT_STAGES`（`docs-link-check` 之后）。
4. **必须带负向验证**（同 S11 §7 纪律）：注入一个错误计数 / 删掉一篇的状态字段 → 门禁须变红。

#### **D5 · 单文档规模与结构一致性**

- 维持 **≤800 行**（AGENTS §四已定，`test-scale.sh` 只管 `.mbt`，**文档侧无门禁**）→
  **把文档行数纳入 `docs-consistency.sh` ①**（≤800，超出须显式登记「已拆承接篇」）。
- **`S10`（776 行）** 距上限只剩 24 行 → 下次增量**必先拆**（建议按 S11b 模式拆出「落地记录」分册）。
- **`README优化-…`（527 行 / 9 个平级轮次）** → 后续轮次改为「轮次索引行 + 独立承接篇」，
  主篇冻结在「§0–§5 结论 + 轮次索引」。

#### **D6 · 「三段式」显式化为规范**

把 S1–S9 事实上的三段结构写成规范（§7.3），使新篇章**有模板可抄**：

```markdown
## 实现方案    ← 要做什么/为什么（含验收标准）
## 实现记录    ← 实际怎么做的（含与方案的偏离及原因）
## 评审与优化  ← 复核发现与结论去向
```

- **保留各篇已有的一字不改**（S11 §6.3 D7：阶段文档是移植可读性资产）；
  规范只约束**新增**篇章与**修订时**的增量段落。
- `S8/S9g/S9h` 等未走三段式的篇章**不强制回改**（回改成本 > 收益），
  仅在其头部 `状态：` 字段中登记结构差异。

### 6.3 P2：评估后不建议做（写在方案里，避免反复讨论）

| # | 想法 | 判断 |
|:-:|------|------|
| D7a | 把 `S9` 18 篇合并为 3 篇 | **不做**：S11 §6.2 D5 已评估（需重写交叉引用，死链风险高）；且 §3.1 显示它们是**对外性能声明的证据链**，合并会削弱可审计性 |
| D7b | 把 `docs/` 纳入发布归档 | **不做**：`publish-check` ⑤ 已定方案（归档外目标用绝对链接）；纳入归档会暴露维护者上下文且增包体（[发布阻塞项 3-4 落地方案](./mooncakes-发布阻塞项3-4-落地方案.md) 已有结论） |
| D7c | 全库文档翻译为英文 | **不做**：无外部读者需求信号；双语会**翻倍漂移面** |
| D7d | 引入静态站点生成器 / docs 站点 | **不做**：`docs/` 的唯一消费方是仓库内读者与 mooncakes 落地页；引入站点 = 新增构建面与失效面（Anti-Abstraction 反例） |

---

## 7. 建议纳入 `AGENTS.md` 的规范条文（提案，待评审）

> 本节是**可直接粘贴进 `AGENTS.md` §四**的条文草案。本文**不代改 `AGENTS.md`**——
> 它属协作硬约定，应由维护者评审后单独提交（§9 队列 D2）。

### 7.1 文档准入（新增/修订文档前必过）

1. **头部三字段**：`状态`（现行/历史/已失效）、`日期`、`索引`（指明在导航中的归属）。缺一不予合入。
2. **单文件 ≤800 行**；超出须拆出「承接篇」并在两处索引登记。
3. **可复跑原则**：任何**数字结论**必须附复跑命令或指向登记处；无复跑路径的结论降级为「讨论稿」，不入 `docs/`。
4. **不新建「索引的索引」**：导航层次 ≤2 级（`docs/README.md` → 分类 → 篇章）。
5. **不合并已判定解耦的域**：`docs/移植参考/**` 不并入主域。
6. **索引双更新**：`docs/README-导航与索引.md`（全量）+ `README.md`（落地页入口）。

### 7.2 链接纪律（现状已闭环，条文化以免回退）

| 链接方位 | 形态 | 兜底门禁 |
|----------|------|---------|
| `README.md` → `docs/**` / `AGENTS.md`（**归档外**） | **仓库绝对链接** | `publish-check.sh` ⑤ |
| `docs/**` 内部互链（**仓库内导航面**） | **相对链接** | `docs-link-check.sh` |
| 图片资产 `docs/assets/**` | README 用 `/-/git/raw/main/...` 绝对直链 | `publish-check.sh` ⑤（含 HTML `src`） |
| 锚点 | `#fragment` 仅保留在相对链接或渲染面绝对链接 | 人工（渲染面才有效） |

### 7.3 篇章结构模板（新增文档抄这份）

```markdown
# S<N><x> · <语义标题>（不要只写代号）

> 状态：现行|历史|已失效　｜　日期：YYYY-MM-DD　｜　索引：<导航> §N
> 承接：[上位篇]　｜　并入：[若为历史，写明结论去哪了]
> 一句话结论：<本页唯一的 TL;DR>

## 实现方案 / 议题定义     ← 要做什么、为什么、验收标准（含「明确不做」）
## 实现记录 / 证据         ← 实际怎么做的、可复跑命令、与方案的偏离及原因
## 评审与优化 / 结论去向    ← 复核发现、结论去处、后续队列
```

### 7.4 一致性收敛（把「人巡检」换成「门禁」）

1. **数字**：只在登记处更新；`docs-consistency.sh` ② 与实跑比对。
2. **状态**：状态变更必须**同时**移动「引用它的现行篇」的表述，否则门禁 ① 红。
3. **索引**：新增篇章未入索引 → 门禁 ③ 红。
4. **新增门禁必须带负向验证**（注入错误 → 必须变红），与 S11 §7 纪律一致。

---

## 8. 验证（实跑记录）

| # | 命令 | 结果 |
|:-:|------|------|
| 1 | `moon test` | `Total tests: 147, passed: 147, failed: 0.` |
| 2 | `bash scripts/docs-link-check.sh` | 扫描 **65** 个 Markdown / **784** 条相对链接 → **零死链** |
| 2b | `bash scripts/docs-consistency.sh` | ①状态 59 篇 ✅ / ②计数 147=147 ✅ / ②b版本 ✅ / ③索引 59 篇（含摘要区按名链接）✅ / ③b 索引反查 ✅ / ③c 无子目录 README ✅ / ④规模 ✅ |
| 3 | `bash scripts/test-scale.sh` | 全部测试文件在限内（合计 5,883 行） |
| 4 | `bash scripts/publish-check.sh` | 归档 34 项 ✅ / 白名单 ✅ / 元数据 ✅ / 发布面链接 ✅ / 质量基线 ✅ |
| 5 | `git ls-files '*.md' \| wc -l` | 64（含 `README.mbt.md` 符号链接 → 实体 63） |
| 6 | `git ls-files -z 'docs/*.md' \| xargs -0 grep -h '^## ' \| sort \| uniq -c \| sort -rn` | `实现方案`/`实现记录` 各 **12** 次（§3.2） |

> 本文档**未改动任何代码、脚本、公共 API、快照、用例**；新增文件仅本篇 + 索引/README 的登记行，
> 故 §8 的门禁结果在本文提交后应保持一致（`docs-link-check` 会因新增篇目而增 1 篇扫描数）。

---

## 9. 分步队列（按风险升序，每步单独 PR）

| 序 | 动作 | 对应 | 风险 | 状态 | 验收 |
|:--:|------|:----:|:----:|:----:|------|
| 1 | **本文 + 索引登记**（`docs/README-导航与索引.md` §6 加一行、`README.md` 文档索引加一行） | S12 | 极低 | ✅ PR #79 | `docs-link-check.sh` 零死链 |
| 2 | **D1 状态头**：为 `docs/**` 补齐 `状态`/`日期`/`索引` 三字段（脚本辅助生成，人工校值） | §7.1 | 低 | ✅ 已落地（59 篇） | `docs-consistency.sh` ① 绿 + 负向验证红 |
| 3 | **D3-A 标题化**：索引表与各篇 H1 改用语义名（文件名不动） | §6.1 | 低 | ✅ 已落地（§7 表全量重写） | 人工抽查 + 死链零 |
| 4 | **D4 门禁**：落地 `scripts/docs-consistency.sh` 并接入 `gates.sh` | §6.2 | 中 | ✅ 已落地（4 断言 + ②b） | 四项断言齐备；注入错误必红 |
| 4b | **D4b 数字单点化**：测试计数登记处 + `146` 漂移收敛 + 版本号不写死 | §6.2 | 中 | ✅ 已落地 | ② 计数一致；②b 版本绿 |
| 5 | **D6 模板**：把 §7.3 写入 `AGENTS.md` §四（供新篇对照） | §7.3 | 中 | ✅ 已落地（§4.1–§4.4） | 约定评审通过 |
| 6 | **D2 目录分类**（**唯一高改动量项**）：47 篇归类 + 全库改链 | §6.1 | **高** | ⏳ 待排期 | `docs-link-check` 全绿 + 人工抽查 10 条锚点 |
| 7 | D5 拆 `S10` 与 `README优化` 的增量（**仅在下次有增量时触发**） | §6.2 | 中 | ⏳ 递延（见 §12） | 各篇 ≤800 行 |
| 4c | **D4c 门禁加固**：修 ② 宽匹配假绿 / PATH 误诊、③ 改「路径精确 + 摘要区双层」、**新增** ③b 索引反查与 ③c 无子目录 README、④ 补「文件消失」判定 | §13 | 中 | ✅ 已落地（PR #81） | 七项断言齐备；**负向 + 正向双跑**全绿 |
| 4d | **文档侧真 bug**：修 `S11 §9.1/§9.2` 死引用；索引 §8 行数漂移收敛（`S10`/`S12`/`moonbit-实现布局`） | §13.4 | 低 | ✅ 已落地 | `docs-link-check` 零死链；行数一致 |
| 8 | **E1 章节引用门禁**（`S<N>x §M` 须在被引篇目存在） | §14/§15.2 | 中 | ✅ 已落地（PR #82） | 100 处活引用全绿；注入 `S11 §9.9` 必红 |
| 9 | **E3 索引 §8 行数改为生成物**（`scripts/docs-index.sh`） | §14/§15.1 | 低 | ✅ 已落地（PR #82） | 生成物与现状一致；消 `S12` 602→607 |
| 10 | **E2 状态头日期漂移提示**（`scripts/docs-date-check.sh`，非阻断） | §14/§15.3 | 低 | ✅ 已落地（PR #82） | 三重噪音抑制后 6 篇 → 归零 |

> **执行顺序理由**：先「立规范」（1–5，零移动）再「动文件」（6）——
> 若先动文件再改规范，会**再漂一次**（这正是 §3.3 的教训）。
>
> **落地现状（2026-09-18）**：1–5、4b **已全部落地**（零文件移动，故对既有引用零影响），见 §12；
> 4c/4d（**门禁自身审计与加固**）已落地，见 §13；**8/9/10（E1/E3/E2）已落地**，见 §15。
> **D2 目录分类仍未做**——它需要另立 PR（唯一高改动量项）。
> **建议顺序**：8（E1）→ 9（E3）→ 10（E2）→ 6（D2）——先把「漂移交给机器」，再动文件（与本节执行顺序理由同源）。

---

## 10. 与既有文档的关系

- **不替代** [S11](./S11-无效代码与冗余文档清理评估.md)：S11 管「留什么/删什么」，本文管「留下的怎么组织」。
  本文的 D4 是 S11 §7-6「不要为了看起来干净删有信息量的历史」的**机制化版本**（用门禁代替人巡检）。
- **不替代** [README优化-冗余清理与最佳实践](./README优化-冗余清理与最佳实践.md)：
  该文记录 **README 单文件**的 9 轮优化；本文把同一套纪律（结论 + 出处、计数不进正文）
  **推广到 `docs/` 全库**。
- **不替代** [docs/README-导航与索引.md](./README-导航与索引.md)：该文是**导航面**（给路径），
  本文是**规范与队列**（给标准）；D2 落地后，该文将成为 `docs/README.md`（目录首页）。
- **约束继承**：发布面链接策略（README 用绝对链接）以
  [README优化 §9](./README优化-冗余清理与最佳实践.md#9-第六轮2026-09-14-v6发布面可达性闭环链接策略--发布状态--门禁-) 为准；
  文档规模/测试纪律以 [AGENTS.md](../AGENTS.md) 与 [S10](./S10-测试用例设计与完善roadmap.md) 为准。
  本文若与它们冲突，**以它们为准并回改本文**。

---

## 11. 参考（外部一手来源）

- **GitHub Spec Kit** · `spec-driven.md`（SDD 方法论全文：权力反转、九条宪法、Phase -1 Gates、层级化细节）
  <https://github.com/github/spec-kit/blob/main/spec-driven.md>　｜　`README.md`（三入口工作流与 `converge`）
  <https://github.com/github/spec-kit>　（**2026 实读**，无版本锚点，引用时注明）
- **Diátaxis**（文档四象限：Tutorial / How-to / Reference / Explanation，用于「混写」判别）
  <https://diataxis.fr/>
- **MoonBit 官方** · 模块与包配置、`mbt check` 文档测试（本项目「可执行规格」的官方依据）
  <https://docs.moonbitlang.com/zh-cn/latest/toolchain/moon/module.html>
  <https://docs.moonbitlang.com/zh-cn/latest/language/docs.html>
- **本项目既有判定口径** · [S11 判定方法](./S11-无效代码与冗余文档清理评估.md#1-判定方法可复跑)（可复跑纪律的出处）

---

## 12. 落地记录（2026-09-18，队列 1–5 + 4b）

> 本节记录 §9 队列 1–5 与 4b 的**实做**。全部**零文件移动**，故对既有 ~780 条相对链接零影响。
> 对应队列见 §9；新增/修订规范见 §7 与 `AGENTS.md` §四.1–§四.4。

| # | 动作 | 落地产物 | 实测 |
|:-:|------|---------|------|
| 1 | 本文 + 索引登记 | 本文件、`docs/README-导航与索引.md` §6/§7/§8 | `docs-link-check` 零死链 |
| 2 | **D1 状态头** | `docs/**` 59 篇补 `状态`/`日期`/`索引` 三字段 | 门禁 ① 绿（59 篇） |
| 3 | **D3-A 标题化** | 索引 §7 拆 4 小节、全量语义标题（文件/`S` 代号不动） | 门禁 ③ 绿 |
| 4 | **D4 门禁** | `scripts/docs-consistency.sh`（① 状态 ② 计数 ②b 版本 ③ 索引 ④ 规模） | 四项断言齐备 |
| 4 | D4 接入 | `scripts/gates.sh` `DEFAULT_STAGES` 加入 `docs-consistency` | 全链绿 |
| 4b | **数字单点化** | `S10b` 顶部 `<!-- test-count: 147 -->`；`146` 由 11 篇压到「登记处 + 历史时点数」 | 门禁 ② 绿 |
| 4b | 版本号不写死 | `README.md` 改「已发布（首个版本）」；门禁 ②b 读 `moon.mod` 比对 | 门禁 ②b 绿 |
| 5 | **D6 规范条文** | `AGENTS.md` §四.1 准入 / §4.2 单点化 / §4.3 模板 / §4.4 链接纪律 | 硬约定生效 |

**负向验证（必跑，S11 §7 纪律）**：

| 注入 | 期望 | 实测 |
|------|------|------|
| 删 `S1` 的状态字段 | ① 红 | ✅ 红 |
| `历史` 篇删 `并入：` | ① 红 | ✅ 红（S9b） |
| 登记处写 `test-count: 146` | ② 红 | ✅ 红（declared=146 vs actual=147） |
| `README` 写死「发布状态 \`0.1.0\`」 | ②b 红 | ✅ 红 |
| 新造一篇不入索引 | ③ 红 | ✅ 红 |
| 给 `S9` 灌到 1010 行 | ④ 红 | ✅ 红（超限 210） |

**未做**：D2 目录分类（§9 序 6，高风险，需另立 PR）；D5 拆篇（§9 序 7，
`S10` 779 / `S9d` 757 / `S9c` 730 行——**仍在 800 限内**，但余量已薄，下次增量先拆）。

> **一句话**：本文第 1 个 PR 只写「诊断与规范」；本轮的落地把其中**零风险部分**（1–5 + 4b）
> 真正做进仓库，并**用门禁替代人巡检**——即 SDD `implement → converge` 的机制化（§4 P5）。


---

## 13. 复核与加固（2026-09-18，第二轮：门禁自身审计）

> §12 的落地把「人巡检」换成了门禁，但**门禁本身也需要被审计**。
> 本节记录对 `scripts/docs-consistency.sh` 的**独立复核**：先复现，再修，再补。
> 全部改动**零文件移动、零代码改动**（只动脚本 + 文档）。

### 13.1 复现的口径缺陷（PR #80 之后的独立核验）

| # | 现象 | 根因 | 危害 |
|:-:|------|------|------|
| B1 | `moon` 不在 `PATH` 时，② 打印 `line 76: Total: command not found`，报错**指向工具链缺失** | `export PATH="$HOME/.moon/bin:$PATH"` 不足以覆盖「宿主未装工具链」的调用面 | **误诊**：README 让读者装工具链/开替我上班，而真因可能只是 PATH；也让 CI 外调用者困惑 |
| B2 | ② 的 `sed -E 's/.*Total tests: ([0-9]+).*/\1/p'` 用 `.*` **宽匹配** | moon 失败输出的**末行提示语本身含** `Total tests: ...`，被当作计数行 | **假绿**：解析失败时 `actual` 看似有值，「计数漂移」这道闸门**静默失效** |
| B3 | ③ 只用 `grep -qF "$base"` 判「是否入索引」 | basename 子串匹配 | **假绿两种**：① 文件未被索引、但 basename 恰好出现在别处（如提到篇名）也算过；② 索引里写了、文件已删（**反向缺口**），`docs-link-check.sh` 遍历的是**实际文件**，看不到 |
| B4 | ③ 只判「出现在索引里」 | 判据过宽 | 把一篇**只塞进 §8 全量清单**的文档也算「已入索引」——可发现性并未达成（§3.4 的可读性问题原样保留） |
| B5 | ③ 的 basename 比较对 `docs/移植参考/**` **误判** | 该 12 篇在索引里写作 `./移植参考/xxx.md`（**带子目录**），basename 集合匹配不到 | **假红**：③ 上线即报 12 处「未入索引」，而它们其实**已被 §7.4 逐篇显式链接** |
| B6 | ③c（本轮新增）最初写成 `git ls-files 'docs/*/*/*.md' 'docs/*/*/*/*.md'` | glob 深度**只到三层**，而 `docs/移植参考/README.md` 恰好是三层 | **漏检**：这条 glob 唯一要抓的就是它，却抓不到（自证式漏检） |

> **B2 + B3 + B5 是同一条教训的三个面**：**判据本身要有反例**。
> 只跑「期望变红」的负向验证不够，还要跑「期望**不变红**」的正向回归（B5 就是被正向回归抓到的）。

### 13.2 加固内容（`scripts/docs-consistency.sh`）

| 断言 | 变更 | 说明 |
|------|------|------|
| ② | `.*Total tests:` → 行首锚定 `^Total tests: [0-9]+` | 只认 moon 的**计数行**；解析不到就 **fail**，绝不静默 |
| ② | PATH 注释显式说明「强制补 `$HOME/.moon/bin` 的原因」 | 消除 B1 误诊 |
| ③ | 正反向**双层判据**：L1 存在性（**归一化路径精确相等**，非 basename）+ L2 摘要区（须在 §0–§6 或状态头 `索引：… §N` 出现） | 修 B3/B4/B5；「入索引」= 读者路径/议题表里点得到 |
| ③b | **新增**：索引里每条 docs 相对链接回查文件系统 | 补反向缺口（B3-②） |
| ③c | **新增**：`docs/**` 禁第二个 `README.md`（「索引的索引」，AGENTS §四 准入 4） | 判据用 `git ls-files docs` + path 过滤，**不再用深度 glob**（修 B6） |
| ④ | 单篇 ≤800 行 + **「git 已登记但工作区缺失」** 显式报错 | 修「文件消失」被误报为超限之类的混淆 |

**特例保留（写明理由，防止后续被当成 bug 改掉）**：`docs/移植参考/**` 12 篇**继续由域首页
`fast-qr-索引.md` 统辖**（S11 §6.3 已判定「外部语料自成一域、不并主域」）。
门禁 ③c **只禁新增子目录 `README.md`**，**不要求**它们各自单列进顶层索引——
这与 §2.2 边界「不照搬外部分类」一致。

### 13.3 负向 + 正向验证（全部实跑）

| 注入 | 期望 | 实测 |
|------|------|------|
| 删 `S1` 状态字段 | ① 红 | ✅ 红 |
| 登记处写 `test-count: 146` | ② 红 | ✅ 红（declared=146 vs actual=147） |
| `env -i` 剥掉 PATH（模拟无 moon） | ② 红且**报错指向计数解析**，不再指向工具链 | ✅ 红（行首锚定生效） |
| 索引塞 `./S99-不存在的文档.md` | ③b 红 | ✅ 红（`README-导航与索引.md:223`） |
| 新增一篇**只进 §8 清单**的文档 | ③ L2 红 | ✅ 红 |
| `docs/移植参考/README.md` 造第二个 README | ③c 红 | ✅ 红 |
| `docs/移植参考/**` 现状 12 篇（**正向回归**） | ③ **不红** | ✅ 绿（B5 已修） |
| 全库现状 | ①②②b③③b③c④ **全绿** | ✅ 绿 |

### 13.4 顺带修掉的文档侧真 bug

- S12 §3.3 / §6.3 引用 **`S11 §9.2` / `S11 §9.1`** —— S11 的 §9 **无子节**（§9 只有一节）。
  已改为真实出处：[README优化 §9](./README优化-冗余清理与最佳实践.md#9-第六轮2026-09-14-v6发布面可达性闭环链接策略--发布状态--门禁-)（不写死线上版本）
  与 [发布阻塞项 3-4 落地方案](./mooncakes-发布阻塞项3-4-落地方案.md)（归档面）。
  **这类「章节引用漂移」整链门禁目前抓不到**（`docs-link-check` 只管文件级链接），
  是下一轮可立项的门禁候选（§14）。

### 13.5 结论

- §12 的「用门禁替代人巡检」方向**成立**，但**门禁的第一版有 3 类假绿 1 类假红**；
  本轮已全部关闭，并给出「负向 + 正向」双跑纪律。
- `docs/移植参考/**` 的定位**明确化**：域首页 `fast-qr-索引.md` 即其索引，**不另建子目录 README**。
- **仍未做**：D2 目录分类（§9 序 6）。**建议**：D2 落地时，把「门禁 ③ 的 L2 摘要区覆盖」
  作为迁移验收项——**每篇在摘要区都能点得到**，才算 D2 真正提升了可发现性。

---

## 14. 后续可立项（供评审取舍）

| # | 提案 | 收益 | 风险 |
|:-:|------|------|------|
| E1 | **章节引用门禁**：校验 `S<N>x §M` 形式的引用在被引篇目内**存在该节** | 抓 §13.4 那类漂移 | 中（需容忍标题写法差异：`### 3.2` 与 `#### 3.2` 并存） |
| E2 | **状态头日期漂移提示**（仅警告不阻断）：状态头 `日期` 早于该文件最后一次实质 commit | 让「末次实质修订」名副其实 | 低（git 时间受 rebase 影响，故只提示） |
| E3 | **索引 §8 行数改为生成物**：`scripts/docs-index.sh` 生成该表，门禁校验一致 | 消灭 §8 里 3 处行数漂移（本轮实测：`S10` 778→779、`S12` 474→521、`moonbit-实现布局` 461→462） | 低 |
| E4 | D2 目录分类（§9 序 6） | 读者 3 秒定位 | **高**（全库改链，须单独 PR） |

> **建议排序**：**E3 → E1 → E2 → D2**。理由：E3/E1/E2 都是**零文件移动**的「把漂移交给机器」，
> 先做完这三项，D2 迁移时才有可靠的安全网（这正是 §9「先立规范再动文件」的同一逻辑）。

---

## 15. E3 / E1 / E2 落地记录（2026-09-18，第三轮：把三类漂移交给机器）

> §14 排的 **E3 → E1 → E2** 三项「零文件移动」的补门禁，本轮全部落地。
> 全程**未移动/改名任何文件**；`lib/**`/`cmd/**`/`.mbt`/黄金值/.mbti **零改动**。

### 15.1 E3 · 索引 §8 改为生成物

| 项 | 内容 |
|----|------|
| 产物 | `scripts/docs-index.sh`（`--print` / `--write` / `--verify` 三模式） |
| 真值源 | `git ls-files -z 'docs/*.md'`（篇数顺序）+ 状态头（`状态`）+ `wc -l`（行数）——**零手抄** |
| 接入 | `gates.sh` 新增 `docs-index` 阻断阶段；索引 §8 说明改为「生成物 + 勿手改」 |
| 消除的漂移 | 实测 `S12` **602 → 607**（本轮前已漂）；并**持续**兜住后续每次修订 |

> **纪律化**：索引 §8 从此是**派生视图**，人只执行 `--write` 搬运，不手改数字
> （与 §6.2 D4「数字单点化」同源）。生成块用 `<!-- docs-index-table: generated -->` /
> `<!-- /docs-index-table -->` 标记成对界定，`--verify` 只比对表行与 `docs-index-count`。

### 15.2 E1 · 章节引用门禁

| 项 | 内容 |
|----|------|
| 产物 | `scripts/docs-ref-check.sh` |
| 判据 | 文中 `S<N>[a-z] §M[.K]` 引用，须在被引篇目内**存在该编号标题**（`## M` / `### M.K` 皆认） |
| 保守性 | ① 只认能映射到 `docs/` 实文件的代号，**泛指代号（如 `S9x`）跳过**；② 剥离行内 code span——反引号内是「对失效引用的引述」（如 §13.4 记录的死引用），非活引用 |
| 接入 | `gates.sh` 新增 `docs-ref-check` 阻断阶段 |
| 实测 | **100 处活引用全绿**；`S11 §9.1/§9.2` 系列（§13.4 已定性）在反引号内，正确跳过 |

> **修掉的判据自证 bug（负向首跑暴露）**：最初标题正则写成
> `^#{1,6}\s+(\d+(?:\.\d+)*)(?![\d.])`，把 `## 1. 产物` 里的 `1.` 判为「编号以 `.` 结尾」→
> **65 处假红**。改为 `(\d+(?:\.\d+)*)\.?(?![\d])` 后降到 4 处，再剥离 code span 后归零。
> 教训与 §13 同：**判据必须双向跑**，只跑「期望红」会漏掉「判据本身写错」。

### 15.3 E2 · 状态头日期漂移提示

| 项 | 内容 |
|----|------|
| 产物 | `scripts/docs-date-check.sh`（**永不 exit 1**，除文件系统级错误） |
| 口径 | 状态头 `日期` < 该篇**末次实质 commit** 日期 → 提示 |
| 噪音抑制（三重，实跑调参） | ① 滤「只动状态头」的 commit；② 滤「横切大批量」commit（改动文件数 > `MAX_FILES`，默认 3）；③ 改动行数阈值 `MIN_LINES`（默认 10） |
| 接入 | `docs-consistency.sh` 新增 **⑤（非阻断）**；同时文件头注释写明「为何只提示」 |
| 实测 | 只加 ① → **58/58 篇全提示**（噪音）；加 ②③ 后降到 **6 篇**；修正这 6 篇状态头日期后归零 |

> **为什么只提示不阻断**：`git` 时间受 rebase / checkout / 提交时序影响，不是文档真值的强判据；
> 「实质修订」本身是启发式。误判代价不该是红门禁——这与 §14 E2 立项时的判断一致。
> **副作用自查**：本项**要求 git 历史**（本环境可用）；若在无 `.git` 的归档面运行，
> `git log` 输出为空 → 全部计入「无实质 commit 记录」，静默跳过（不误报）。

### 15.4 负向 + 正向验证（全部实跑）

| 注入 / 场景 | 期望 | 实测 |
|------|------|------|
| 加一行 `S11 §9.9`（不存在节） | `docs-ref-check` 红 | ✅ 红 |
| 加一行 `S12 §77` | 红 | ✅ 红 |
| 加一行 `S99 §1`（泛指/未映射代号） | **不红**（保守跳过） | ✅ 绿 |
| 反引号内写 `S11 §9.1` | **不红**（引述非活引用） | ✅ 绿 |
| 手改索引 §8 行数（S12 607→602） | `docs-index --verify` 红 | ✅ 红 |
| 把 `README优化` 日期改回旧值 | `docs-date-check` 提示（exit 0） | ✅ 提示且不阻断 |
| 全库现状 | 11 阶段全绿 | ✅ 见 §15.5 |

### 15.5 全链实跑（`bash scripts/gates.sh`）

```
fmt-check ✅ → check ✅ → test ✅ → docs-link-check ✅ → docs-ref-check ✅ → docs-index ✅
→ docs-consistency ✅ → test-scale ✅ → build-and-run ✅ → diff-gate ✅ → publish-check ✅
```

- `docs-link-check`：65 文件 / **791** 条相对链接零死链
- `docs-consistency`：①②②b③③b③c④ **七项全绿** + ⑤ 日期提示（COUNT=0）
- `moon test`：**147 全绿**

### 15.6 仍未做 & 下一步

- **D2 目录分类**（§9 序 6 / §14 E4）**仍未做**——唯一高改动量项，须单独 PR。
  现在 E1/E2/E3 已就位，D2 迁移时有三层安全网：**文件级死链**（docs-link-check）、
  **章节引用**（docs-ref-check）、**索引双向一致**（docs-consistency ③/③b）+ 清单自生成（docs-index）。
- **D5 拆篇**：`S10` 779 / `S9d` 757 / `S9c` 730 行仍在 800 限内但余量已薄，下次增量前先拆。
